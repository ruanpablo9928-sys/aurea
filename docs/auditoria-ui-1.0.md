# Auditoria da UI 1.0

Nove frentes auditaram a UI nova contra o motor, com a regra de que
veredicto sem `arquivo:linha` nao vale e que PASS exige citar as DUAS
pontas — o comando no controller e o widget que chama.

Este documento e o resultado bruto. O que foi corrigido esta marcado nos
commits que o citam.

## O que ja ganhou porta

**Mascara (lote 3).** Dos treze comandos listados como ESCONDIDO em
`COMP — Masks` e nas linhas de MASKS da Frente 3, oito viraram controle
no cartao "Mascara" de `categoriasDaCamada`: `addMask`, `removeMask`,
`reorderMask`, `cycleMaskMode`, `toggleMaskInverted`, `editMaskParam`
(as quatro chaves), `toggleMaskFeatherAxes` e `applyMaskReveal` (os
sete presets). O rail esquerdo mira o parametro escolhido por
`toggleMaskParamKeyframe`, que teve de ser escrito: existia toggle so
para o CAMINHO (`toggleMaskPathKeyframe`), nao para os numeros — dava
para arrastar o valor e nunca para cravar o instante.

Continuam sem porta, e de proposito, os tres que dependem de gesto no
palco (`palco_de_previa.dart` ainda nao tem `GestureDetector`):
`editMaskPath`, `replaceMaskPath` e `toggleMaskPathKeyframe`.
`setMaskFromOwnShape` e o rastreio de mascara seguem na fila.

Com isto, quem abrir os modelos de fabrica que ja criavam mascaras
(`dnyx_remix_template.dart`, `reference_rebuild_template.dart`) passa a
**ver, editar e remover** o que antes so podia sofrer.

**Composicao (lote 4).** O cartao "Mistura e recorte" liga `setBlendMode`
e `setCustomBlend` (27 modos, em seis familias) e `setMatteFromAbove`
(os cinco modos de matte, apagados com o motivo escrito quando nao ha
camada com imagem acima). Na categoria "Camada" entraram o parentesco
(`linkProperty(..., LayerProp.parent, ...)` / `unlinkProperty`, com uma
lista de candidatos) e o grupo (`groupLayer`, `enterGroup`).

`exitGroup` ganhou porta na barra de cima: dentro de um grupo, a seta de
voltar vira "Sair do grupo", e o botao de voltar do sistema faz o mesmo
(`PopScope`). Sem isso, entrar num grupo seria uma armadilha — as
edicoes feitas la dentro so voltam para o grupo em `exitGroup`.

**Som (lote 5).** O cartao "Volume" virou "Som" e passou a existir
tambem na camada de AUDIO, que ficava de fora por um `is!`:
`editVideoVolume` recusa o que nao for video, e o cartao segue o
comando — o campo `volume` sempre esteve la, o mixer sempre leu e a
exportacao sempre respeitou. Agora ha `editVolume`.

Dentro dele: volume, entrada e saida (`updateAudioSpec`, limitadas a
metade do clipe), mudo, emparelhar o volume (`normalizeAudio`) e a
familia de batida (`detectBeatsInto`, `clearBeats`, `cutAtMarkers`,
`distributeAtMarkers`).

Duas coisas ganharam DESENHO, e nao so comando:

- **a forma de onda no clipe.** A piramide de picos existia inteira —
  seis niveis de detalhe, cache em disco, escolha de nivel por pixel —
  e nunca foi desenhada. Uma faixa de audio era um retangulo liso: dava
  para ver ONDE o som esta, nunca O QUE ele e. O desenho usa o tempo do
  ARQUIVO (com `sourceOffset` e `speed`), entao aparar revela outro
  pedaco da onda em vez de esticar a mesma.
- **as batidas na regua.** `state.beats` nao era desenhado em lugar
  nenhum: achar as batidas era uma acao sem resposta na tela, e nao
  havia como conferir a grade antes de mandar cortar.

**Video: velocidade e a alca que passava do fim (lote 6).** Fecha os
dois itens marcados **QUEBRADO** na Frente 6 (`VIDEO — Trim` e
`AUDIO — Trim`) e abre a porta de `setClipSpeed`, `setClipReverse`,
`setClipSpeedBlur`, `setClipInterpolacao`, `setClipPreservePitch` e as
quatro rampas de `applySpeedRamp`, no cartao "Velocidade".

A raiz dos dois defeitos era a mesma: **a camada nao guardava quanto o
ARQUIVO tem**. A duracao real chegava do probe no import e era jogada
fora; sobrava so quanto do arquivo estava em uso. `VideoLayer` e
`AudioLayer` passaram a ter `sourceDuration` (persistida como `srcDur`,
opcional — projeto antigo abre igual e, sem a medida, nao ha teto), e
as duas alcas travam nela.

O terceiro defeito da mesma linha do resumo tambem saiu: importar video
custava **dois passos de desfazer**, porque o probe chegava depois da
janela de 450 ms e entrava na pilha sozinho — o primeiro "desfazer"
devolvia o clipe aos 4 s provisorios em vez de remove-lo. E a mesma
correcao sobrescrevia qualquer aparagem feita nesse meio-tempo. Agora a
chegada do probe nao e uma edicao (nao empilha) e so estica o que ainda
esta no provisorio.

**Cor e ponto no efeito (lote 7).** A linha `Cor` era de LEITURA — os
tres numeros e o quadradinho, sem como trocar — com `setEffectColor` e
`setEffectExtraColor` parados sem chamador. Agora ha `EscolhaDeCor`:
doze cores prontas para o caso comum e tres linhas de canal (0..255)
para o resto, na mesma lingua de fita e campo do resto do painel. Sem
roda de cor e sem deslizante.

Os parametros de PONTO (`Centro X`, `Centro Y`) sairam do balde do
"ajuste chega numa proxima entrega": sao numeros de 0 a 1 e viraram
linhas comuns, com fita, campo e losango do rail. A cruz arrastavel
sobre a previa continua sendo o certo, e depende do palco ganhar gesto.

**Selecao multipla (lote 8).** A auditoria chamou isto de raiz
estrutural, e era: `multiSelectProvider` so era LIMPO, nunca preenchido
— nenhum gesto punha duas camadas no conjunto, e por isso `groupLayers`,
`reorderLayers`, `alignSelection`, `distributeSelection`,
`cascadeSelection` e as acoes em lote ficavam todas sem porta.

O toque longo na pilha junta (e a primeira juncao leva a selecionada
junto, senao a pessoa manda juntar duas e ve "uma"). O anel verde no
clipe diz quem vai junto — cor diferente da selecionada branca, porque
sao duas coisas: a branca e a que as ferramentas abrem, a verde e a que
a acao em lote pega. A barra de cima vira "N camadas", e sair dali
desfaz a juncao.

O painel do conjunto e um estado proprio (`EstadoDoPainel.selecao`), e
nao uma categoria: ele nao pertence a camada nenhuma. Traz alinhar (as
seis bordas, pela SELECAO e nao pela composicao — quem juntou duas
camadas quer encostar uma na outra), espalhar (apagado com o motivo
escrito abaixo de tres camadas), escalonar no tempo, subir e descer na
pilha, agrupar, duplicar e apagar. Duplicar e apagar em lote usam
`runAsOneUndo`: apagar cinco e uma acao, e nao cinco.

**Exportacao (lote 9).** `ExportSettings` estava escrito, testado e
completo, e era INALCANCAVEL: o unico construtor real era
`const ExportVideoScreen()`, sem ajuste nenhum, e a tela comecava a
renderizar no `initState` — nao havia momento em que desse para
escolher. Toda exportacao saia no tamanho do projeto, em H.264, na
qualidade media.

Entrou a fase `_Fase.ajustes`, antes de comecar: formato, tamanho, fps,
codec e qualidade, com a previsao de tamanho de arquivo. Codec e
qualidade somem na sequencia PNG, que nao passa por codificador nenhum.
O erro ganhou "Mudar os ajustes e tentar de novo" — quase todo erro
daqui se resolve mudando um ajuste, e repetir igual daria o mesmo erro.

Junto saiu o item **QUEBRADO** `EXPORT — Quality / Bitrate`: havia DUAS
fontes de verdade para o mesmo botao (`ExportVideoScreen.quality` e
`ExportSettings.quality`), e `taxaDeBits` so olhava os ajustes quando
havia taxa na mao, HEVC ou tamanho diferente do original. No caminho
comum, escolher "alta" era silenciosamente ignorado. A `String quality`
da tela morreu, e a formula duplicada de `PlatformEncoder` tambem.

**Linha do tempo: precisao (lote 10).** Quatro itens da mesma familia:

- **`PREVIEW — Current Time` (QUEBRADO)**: `compositionFps` nascia em 30
  e nunca era atribuido. A capsula contava os quadros pelo fps do
  PROJETO e o cabecote encaixava numa grade fixa de 30 — num projeto a
  24 ou a 60, o numero lido e o quadro exportado eram coisas
  diferentes. A tela agora alimenta a grade a cada build.
- **`PREVIEW — Frame Forward/Back` (FALTA)**: `stepFrame` novo, e duas
  setas coladas na capsula. A capsula deixou de ser desenho e virou
  widget porque ganhou controle. O passo anda pelo INDICE do quadro,
  e o instante de um quadro e o TETO da divisao — truncar (ou
  arredondar) fazia a ida e a volta nao fecharem, e o passo ficava
  preso repetindo o mesmo instante.
- **`PREVIEW — Loop` (ESCONDIDO)**: o motor lacava e o unico lugar do
  app que ligava era a tela de estresse 3D. Agora ha um interruptor na
  ponta da barra de rolagem da regua — a unica peca da regua que fala
  da extensao do projeto.
- **imantacao (FALTA, citada no resumo do audio)**: `domain/imantacao.dart`.
  O clipe cola no cabecote, nas duas pontas de cada outro clipe, no
  comeco e no fim do projeto, nos marcadores e nas batidas. Cola pelas
  DUAS pontas — arrastar para encostar o FIM no comeco do proximo e o
  caso mais comum de todos. A tolerancia e em PIXELS: fixa em
  milissegundos seria generosa demais com o zoom fechado e inutil com
  ele aberto.

**A gravacao do projeto (fora da auditoria, achado no caminho).** A
ponte `ref.listen(editorControllerProvider) -> upsert(projetoCompleto)`
morava na tela de edicao antiga e foi apagada com ela: o editor novo
**nao gravava nada em disco**. Voltou, junto com um flush ao ir para o
segundo plano.



---

## Frente 1

## FRENTE 1 — TRANSFORM 2D

| item | veredicto | prova (arquivo:linha) | onde caberia / o que fazer |
|---|---|---|---|
| Position X/Y | PASS | motor `editPosition` em `lib/src/features/editor/application/editor_controller.dart:4000`; UI em `lib/src/features/editor/presentation/widgets/painel_de_transformacao.dart:124` e `:131` (campos x/y) e `:218` (almofada) | — |
| Position Z | PASS (condicional) | motor `editPositionZ` em `editor_controller.dart:6572`; UI `painel_de_transformacao.dart:134-143`, campo apagado quando `!l.is3D` | — |
| Scale X / Scale Y | **QUEBRADO** | `painel_de_transformacao.dart:166-171` (campo Altura) chama `_escalar(scaleX_atual, v/100)`; `_escalar` em `:282-289` ignora o segundo argumento quando a corrente esta travada e chama `editScaleUniform(x)`. `escalaTravadaProvider` nasce **travado** (`:26`) | Corrigir `_escalar`: com a corrente travada, o campo Altura deve chamar `editScaleUniform(y)` (o valor DIGITADO), nao reaplicar o X vigente. Hoje digitar altura com o padrao de fabrica nao muda um pixel |
| Uniform Scale (corrente) | PASS | motor `editScaleUniform` em `editor_controller.dart:4066`; UI `painel_de_transformacao.dart:284` e o botao `_Corrente` em `:161-165` / `:293-330` | — |
| Rotation (Z) | PASS | motor `editRotation` em `editor_controller.dart:4138`; UI `painel_de_transformacao.dart:226-231` (`DialDeAngulo`) | — |
| Rotation X / Y (3D) | ESCONDIDO | motor `editRotationX`/`editRotationY` em `editor_controller.dart:4141` e `:4144`; busca inversa em `presentation/`: **zero chamadas** | O rail direito de `painel_de_transformacao.dart:94-105` ja e a lista de modos; o modo "girar" (`:225-231`) deveria trocar de eixo quando `l.is3D`, como o campo z ja faz em `:140-142` |
| Anchor Point (pivo) | ESCONDIDO | motor `editPivot` em `editor_controller.dart:4181`, com keyframe (`toggleKeyframe` caso `LayerProp.pivot`, `:4405`), curva (`setSegmentEase`, `:4454`) e render aplicado (`palco_de_previa.dart:1200`, `:1302-1307`). Busca inversa: **zero chamadas** em todo `lib/`. Havia porta na UI velha — o helper morto `test/editor_audit_helpers.dart:5-12` ainda procura "Editar pivo" | Falta o 5.o modo no `RailDireito` (`painel_de_transformacao.dart:95-100`) com uma `AlmofadaDeArrasto` igual a de mover. O nome ja esta reservado no motor: `TransformTool.pivot` em `lib/src/features/editor/application/ui/editor_session.dart:13` |
| Opacity | PASS | motor `editOpacity` em `editor_controller.dart:4147`; UI `controles_da_camada.dart:293` e `:295`; categoria propria em `painel_da_camada.dart:118-122` | — |
| Skew (X e Y) | PASS | motor `editSkewX`/`editSkewY` em `editor_controller.dart:4161` e `:4171`; UI `painel_de_transformacao.dart:183`, `:191` (campos) e `:260`, `:274` (fitas) | — |
| Skew Axis | N/A | o Aurea nao modela "skew + eixo": modela DOIS skews independentes, `skewX` e `skewY` (`lib/src/features/editor/domain/layer.dart:88-89`), com duas fitas (`painel_de_transformacao.dart:250-278`). "Skew Axis" e a parametrizacao do After Effects, e nao ha propriedade equivalente nem falta funcao — o par XY cobre o mesmo espaco | — |
| Transformacao pelo CANVAS (arrastar na previa) | ESCONDIDO | a previa e `CompositionView` **sem gesto nenhum** (`editor_screen.dart:220-224`); a borda de selecao e `IgnorePointer` puro (`palco_de_previa.dart:1273-1287`); o proprio arquivo declara a remocao em `palco_de_previa.dart:5-10` e a tela em `editor_screen.dart:18-21`. O unico `editPosition` de palco que sobrou esta em `freehand_overlay.dart:84`, num widget que **nada instancia** | Um `GestureDetector` no `SizedBox` de `editor_screen.dart:208-224` mapeando dedo->coordenada de composicao (a escala ja e so visual, o comentario em `:213-219` garante isso) chamando `editPosition` com `beginGesture`/`endGesture` |
| Controles numericos | ESCONDIDO (parcial) | ha `CampoDeValor` para posicao (`painel_de_transformacao.dart:120-143`), escala (`:155-171`) e inclinacao (`:178-192`). **Rotacao nao tem**: o centro do dial e um `Text` sem toque (`dial_de_angulo.dart:248`), e o painel devolve `SizedBox.shrink()` na fileira de campos do modo girar (`painel_de_transformacao.dart:146-149`) | Trocar o `Text` de `dial_de_angulo.dart:248` por um `CampoDeValor` (ele ja usa `CampoDeValor.raio` na linha 246 — a caixa ja finge ser campo). Sem isso nao ha como cravar 90,0° exatos |
| Reset | ESCONDIDO | motor `resetProp` em `editor_controller.dart:4213`, com caso completo para position/scale/rotation/opacity/skew/pivot. Busca inversa em `lib/`: **zero chamadas** (so `test/beta_3d_stability_test.dart:44`) | O `RailEsquerdo` tem tres botoes — voltar, keyframe, curva (`rails_do_painel.dart:56-89`). Um quarto botao ali, mirando o mesmo `AlvoDoRail` que ja carrega a propriedade do modo (`controles_da_camada.dart:187-237`), resolve sem tela nova |
| Keyframes (marcar/tirar) | PASS | motor `toggleKeyframe` em `editor_controller.dart:4357`; UI `controles_da_camada.dart:225` via `AlvoDoRail` -> losango do `RailEsquerdo` (`rails_do_painel.dart:66-80`) | — |
| Keyframes (mover / apagar na timeline) | PASS | motor `moverKeyframeDeTransformacao` em `editor_controller.dart:4296` e `apagarKeyframeDeTransformacao` em `:4333`; UI `linha_do_tempo.dart:283-288` | — |
| Keyframes (curva / easing por trecho) | PASS | motor `setSegmentEase` em `editor_controller.dart:4412` e `applyEaseToAllSegments` em `:4463`; UI `controles_da_camada.dart:231` e `:233` | — |
| Alternar rapido MOVE / SCALE / ROTATE / ANCHOR | ESCONDIDO | o `RailDireito` existe e troca em um toque, mas com quatro modos que **nao incluem ancora**: mover, girar, escalar, inclinar (`painel_de_transformacao.dart:95-100`, enum em `:15`). ANCHOR nao tem slot | Acrescentar `ModoDeTransformacao.ancora` em `painel_de_transformacao.dart:15`, o icone no rail em `:95-100`, e o caso em `propDoModo`/`nomeDoModo` (`controles_da_camada.dart:119-131`) — os dois switches sao exaustivos e vao apontar o que falta na compilacao |

## FRENTE 2 — TEXTO

| item | veredicto | prova (arquivo:linha) | onde caberia / o que fazer |
|---|---|---|---|
| Editar texto | PASS | motor `editTextLayer` em `editor_controller.dart:5608`; UI `controles_da_camada.dart:380` (`TextField` da categoria "texto", `:326-403`) | — |
| Font (familia) | ESCONDIDO | motor completo: `editTextLayer(fontFamily:)` em `editor_controller.dart:5608-5614`, `TextLayer.fontFamily` em `layer.dart:793`, render em `animated_text.dart:40`, e o servico inteiro `FontService` com `families` (`font_service.dart:33`), `import` (`:90`), `remove` (`:146`). Busca inversa: o **unico** chamador de `FontService` em `lib/` e `main.dart:56` (`loadAll`) | Uma linha de escolha de fonte dentro de `_Texto` (`controles_da_camada.dart:355-401`), acima de "Tamanho", listando `FontService.instance.families`. O importador de .ttf tambem nao tem porta em lugar nenhum |
| Weight (negrito) | PASS | motor `editTextLayer(bold:)` em `editor_controller.dart:5608`; UI `controles_da_camada.dart:394-399` (`_Interruptor` "Negrito") | — |
| Weight (eixo real 100-900) | FALTA | o motor so tem `bool bold` (`layer.dart:787`) e o render colapsa em dois valores: `fontWeight: l.bold ? FontWeight.w700 : FontWeight.w400` (`animated_text.dart:41`). Uma fonte importada com nove pesos so consegue usar dois | Exige campo novo em `TextLayer` e parametro em `editTextLayer`; a porta seria a mesma linha da fonte em `_Texto` (`controles_da_camada.dart:394`) |
| Size | PASS | motor `editTextLayer(fontSize:)` em `editor_controller.dart:5608`; UI `controles_da_camada.dart:383-393` | — |
| Color / Fill | ESCONDIDO | motor `editTextLayer(color:)` em `editor_controller.dart:5608-5614` e `TextLayer.color` em `layer.dart:786`, usado no render (`animated_text.dart:37`). Busca inversa: **nao existe seletor de cor nenhum** em toda `lib/src/features/editor/presentation/` — grep por `ColorPicker`/`Seletor`/`escolherCor` volta vazio; a categoria "Cor e preenchimento" esta na grade DESABILITADA com "Chega numa proxima entrega" (`painel_da_camada.dart:155-161`) | A categoria `cor` ja tem cartao reservado em `painel_da_camada.dart:155-161`: basta ligar `disponivel: true`, um caso `'cor'` em `_conteudo` (`controles_da_camada.dart:83-92`) e um titulo em `tituloDaFerramenta` (`painel_da_camada.dart:391-401`) |
| Opacity (texto) | PASS | e a opacidade da camada: `editOpacity` em `editor_controller.dart:4147`, UI `controles_da_camada.dart:293` | — |
| Alignment | FALTA | `TextLayer` nao tem campo de alinhamento (`layer.dart:784-804`) e os DOIS caminhos de render cravam centro: `palco_de_previa.dart:4019` (`textAlign: TextAlign.center`) e `animated_text.dart:60` | Campo novo em `TextLayer` + parametro em `editTextLayer`; a porta e `_Texto` (`controles_da_camada.dart:355-401`), ao lado do "Negrito" |
| Tracking (estatico) | FALTA | nao ha campo no modelo; o render **crava** `letterSpacing: -l.fontSize * 0.02` (`animated_text.dart:42`). Existe `TextStyleDef.tracking` (`layer_meta.dart:294`) mas ele e serializado (`project_store.dart:2823`) e **nunca lido**: `VideoProject.textStyleFor` (`video_project.dart:211`) tem zero chamadores em todo o repositorio | Campo em `TextLayer` + `LinhaDeParametro` em `_Texto` (`controles_da_camada.dart:383`, mesmo molde de "Tamanho") |
| Tracking (animado, por unidade) | ESCONDIDO | `TextAnimProp.tracking` existe (`text_animator.dart:628`) e o render aplica de verdade (`animated_text.dart:140-141`, `:169`, `:243`). Comando `toggleAnimatorPropType` em `editor_controller.dart:5442` — zero chamadores | ver linha "ANIMACAO DE TEXTO" abaixo |
| Leading (entrelinha) | FALTA | sem campo no modelo; render crava `height: 1.1` (`animated_text.dart:43`). `TextStyleDef.lineHeight` (`layer_meta.dart:295`) e o mesmo caso morto do tracking | igual ao tracking: campo + linha em `_Texto` |
| Anchor (do texto) | ESCONDIDO | e o pivo da camada — `editPivot` em `editor_controller.dart:4181`, sem porta (ver frente 1). O `TextBoxSpec.anchor` (`layer_meta.dart:340`) e escrito por `setTextBox` (`editor_controller.dart:887`) mas nada le | modo ancora no `RailDireito` (`painel_de_transformacao.dart:95-100`) |
| Stroke (contorno do texto) | FALTA | `TextLayer` nao tem stroke (`layer.dart:784-804`); o render usa um `TextStyle` sem `foreground`/`shadows` (`animated_text.dart:35-53`). `ShapeStroke` (`editor_controller.dart:6278-6303`) so serve `ShapeLayer`. No catalogo de efeitos so ha `findEdges` (Sobel), que nao e contorno de glifo (`domain/effect.dart:1376-1382`) | Cartao "Borda e sombra", ja reservado desabilitado em `painel_da_camada.dart:162-168` — mas exige o campo no motor antes |
| Text box vs point text | FALTA (campo inerte) | `TextBoxSpec` com `mode`/`width`/`height`/`anchor` existe (`layer_meta.dart:335-359`) e `setTextBox` escreve (`editor_controller.dart:887`), mas os unicos leitores em `lib/` sao a serializacao (`project_store.dart:2509-2514`, `:2576-2578`). Nenhum render ou layout consulta `meta.textBox`: o texto e sempre auto-width, medido pelo `TextPainter` sem `maxWidth` (`animated_text.dart:57-61`) | O campo ja existe; falta o motor honrar (quebra de linha por largura) antes de valer a pena por controle. Porta natural: `_Texto` em `controles_da_camada.dart:355-401` |
| Character / Word / Line (unidade) | ESCONDIDO | `TextAnimUnit` com character/charactersNoSpaces/word/line/all (`domain/text_anim.dart:32`), rotulos prontos (`:34-40`), `basedOnFor` (`:42-48`), segmentacao real (`TextUnits`, usada em `animated_text.dart:63`) e o comando de contagem `textAnimUnitCount` (`editor_controller.dart:5361`). Zero chamadores fora do controller | ver linha "ANIMACAO DE TEXTO" |
| ANIMACAO DE TEXTO — Entrance / Exit / Emphasis | ESCONDIDO | catalogo de **35 animacoes** (`domain/text_anim.dart:156`, `textAnimCatalog`), tres posicoes (`TextAnimSlot` em `:23`), e o comando `setTextAnim` em `editor_controller.dart:5377` + `updateTextAnim` `:5393` + `setTextAnimParam` `:5406`. O render honra tudo (`TextLayer.effectiveAnimators` em `layer.dart:808-811`, `hasTextAnimation` em `:815-818`, pintor em `animated_text.dart:84-250`). Busca inversa: **zero chamadores** de qualquer um desses comandos em `lib/` fora do proprio controller | Falta a categoria: `categoriasDaCamada` (`painel_da_camada.dart:112-174`) da a `TextLayer` **apenas** o cartao "texto" (`:123-128`). Cabe um cartao `animacao` ao lado dele, com `_conteudo` novo em `controles_da_camada.dart:83-92` e titulo em `painel_da_camada.dart:391-401`. `EditorPanel.animators` ja esta reservado em `application/ui/editor_session.dart:33` |
| Stagger (atraso entre unidades) | ESCONDIDO | `TextAnim.stagger` (`domain/text_anim.dart:101`, default por spec em `:81`), usado em `totalFor` (`:694`) e na compilacao dos seletores. Editavel por `updateTextAnim` (`editor_controller.dart:5393`) — sem chamador | mesmo cartao de animacao acima; e um dos "seis controles" que o proprio comentario do motor promete (`text_anim.dart:635-636`) |
| Presets | ESCONDIDO | sete presets prontos em `domain/text_presets.dart:20` (`textPresets`) e o comando `applyTextPreset` em `editor_controller.dart:5254`. **Zero chamadores** | uma fileira de presets no topo do cartao de animacao; alternativamente na barra de familias de `adicionar_conteudo.dart:148-158`, onde hoje "Texto" so chama `addTextLayer` |
| Custom (animadores crus + seletores) | ESCONDIDO | pilha completa sem porta: `addTextAnimator` `:5414`, `removeTextAnimator` `:5426`, `toggleTextAnimator` `:5437`, `toggleAnimatorPropType` `:5442`, `editAnimatorPropValue` `:5456`, `addTextSelector` `:5503`, `removeTextSelector` `:5514`, `cycleSelectorMode` `:5540`, `setRangeSelectorShape` `:5555`, `editSelectorParam` `:5568` — todos em `editor_controller.dart`, todos com zero chamadores. Sao **16 propriedades animaveis por unidade** (`text_animator.dart:622-643`), inclusive 3D | modo avancado do mesmo cartao de animacao; `EditorPanel.animators` (`editor_session.dart:33`) ja nomeia o lugar |
| Keyframes de animacao de texto | ESCONDIDO | `toggleAnimatorPropKeyframe` em `editor_controller.dart:5478` — zero chamadores. O `AlvoDoRail` (`controles_da_camada.dart:187-237`) so sabe montar alvo para `LayerProp` e para parametro de efeito (`:245-270`); nao ha caso para propriedade de animador | Um terceiro construtor de `AlvoDoRail` em `controles_da_camada.dart`, no mesmo molde de `alvoDoParametroDeEfeito` (`:245`), mirando o `AnimatorProperty` escolhido |
| Text on a path (bonus, fora da lista) | ESCONDIDO | `TextPathSpec` (`domain/text_path.dart`), render ativo (`animated_text.dart:66-70`, `palco_de_previa.dart:4006-4016`) e comando `updateTextPath` em `editor_controller.dart:5354` — zero chamadores | mesmo cartao de animacao / texto |

---

## Frente 2

## Auditoria — frente EFFECTS + COLOR

**Legenda dos caminhos** (todos sob `C:/Users/SnyX/Documents/Projetos - Claude/Aurea/`):
- `CTRL` = `lib/src/features/editor/application/editor_controller.dart`
- `UI` = `lib/src/features/editor/presentation/widgets/controles_da_camada.dart`
- `SPEC` = `lib/src/features/editor/domain/effect.dart`
- `PALCO` = `lib/src/features/editor/presentation/widgets/palco_de_previa.dart`
- `PAINEL` = `lib/src/features/editor/presentation/widgets/painel_da_camada.dart`

**Método**: para cada comando de efeito/cor do motor, busca inversa `grep <nome> lib/src/features/editor/presentation/`. Resultado bruto da varredura: dos 17 comandos de efeito do controller, **apenas 5** têm chamador na UI nova (`addEffect`, `removeEffect`, `toggleEffectEnabled`, `editEffectParam`, `toggleEffectParamKeyframe`). A única superfície de efeito no app é `_Efeitos` (UI:492) — nem o cabeçalho, nem a pilha de camadas, nem o `+` redondo tocam em efeito (varredura em `editor_screen.dart`, `rails_do_painel.dart`, `visao_geral_das_camadas.dart`, `linha_do_tempo.dart`, `adicionar_conteudo.dart`: zero ocorrências).

### EFFECTS

| item | veredicto | prova (arquivo:linha) | onde caberia / o que fazer |
|---|---|---|---|
| **Add Effect** | PASS | motor `CTRL:4507` `addEffect`; UI `UI:946` (chip do catálogo) e `UI:527` (botão "Adicionar efeito") | Funciona. Ressalva dura: o catálogo despeja os 56 specs sem filtro, incluindo `timeRemap` (`SPEC:38`), que o resto do motor gerencia por caminho próprio (`CTRL:2944` `replaceTimeRemap`), e os 8 tipos que só existem como transição (`essential_warp_pass.dart:9`). Adicionar `timeRemap` pela lista é uma porta para um estado que o motor não espera — filtrar em `UI:901`. |
| **Search** | ESCONDIDO | dado existe: `SPEC:298` `synonyms` (56 specs, ~350 sinônimos, com "green screen", "bloom", "lift gamma gain"); único consumidor é `lib/src/features/help/presentation/quick_guide_screen.dart:34`, tela da aba **Sobre**, que só explica e não adiciona; `UI:898-990` não tem `TextField` | Um `TextField` no topo de `_CatalogoDeEfeitos`, logo acima do `_Acao` de voltar (`UI:918`), filtrando `name + category + synonyms`. Achar "Glow" hoje é rolar 56 chips num `Wrap`. |
| **Categories** | PASS | `SPEC:296` `category`; `UI:901-905` agrupa por categoria e ordena | Ok. Ordenação alfabética crua ("Blur, Color, Distort, Generate, Keying, Lens, Light, Stylize") — aceitável. |
| **Enable/Disable** | PASS | motor `CTRL:4599` `toggleEffectEnabled`; UI `UI:516`; render respeita em `PALCO:1651` | Ok. |
| **Remove** | PASS | motor `CTRL:4576` `removeEffect`; UI `UI:519` | Ok. |
| **Reorder** | ESCONDIDO | motor `CTRL:4586` `reorderEffect(layerId, effectId, delta)`; **zero chamadores** em toda a `lib/`; render honra a ordem da lista em `PALCO:1650` | Duas setas ▲▼ ao lado dos dois `_IconeDoTitulo` de `_TituloDoEfeito` (`UI:594-620`). A ordem muda o resultado (Glow antes ou depois de Levels é outra imagem) e hoje só se muda apagando e reaplicando. |
| **Duplicate** | ESCONDIDO | motor `CTRL:4555` `duplicateEffect` (copia valores **e** keyframes, insere logo abaixo — o próprio comentário `CTRL:4551-4554` diz "dois Glows um sobre o outro é a receita de brilho mais usada que existe"); **zero chamadores** | Terceiro ícone em `_TituloDoEfeito` (`UI:594-620`), ao lado de olho e lixeira. |
| **Copy** | FALTA | não existe comando de copiar efeito no `CTRL`; o mais perto é `CTRL:700` `saveEffectPresetFrom(layerId, name, onlyEffectIds)`, que exige nome e grava preset em disco | Ou expor `saveEffectPresetFrom` como "Copiar" com nome automático, ou acrescentar um `EffectInstance? _areaDeTransferencia` no controller. Porta: `_TituloDoEfeito` (`UI:594`). |
| **Paste** | FALTA | não existe; `CTRL:725` `applyPreset(layerId, preset, at:, replace:, stretchTo:)` faz o trabalho pesado (reconcilia, renormaliza por tamanho, estica keyframes) mas precisa de um `EffectPreset` que ninguém produz na UI | Segundo `_Acao` ao lado de "Adicionar efeito" em `_Efeitos` (`UI:525-531`), habilitado só quando há algo copiado. |
| **Reset** | FALTA | não há reset de parâmetro nem de efeito no motor; `CTRL:4213` `resetProp` só cobre `LayerProp` (posição, escala…) e **também não tem chamador**; `SPEC:205` guarda `initial` de cada parâmetro, então o reset é composição trivial de `editEffectParam` | Toque longo no `_Chip` de `LinhaDeParametro` (`linha_de_parametro.dart:104`) volta ao `def.initial`; e um `_Acao` "Reiniciar efeito" no fim de `_ParametrosDoEfeito` (`UI:744-757`). |
| **Presets** | ESCONDIDO | **dois sistemas inteiros no escuro**. (a) profundidades: `SPEC:311` `presets` (3 `EffectPronto` em ~40 specs) + `SPEC:237` `EffectDepth` + `CTRL:4537` `applyEffectPronto` + `CTRL:4522` `setEffectDepth` — zero chamadores; o campo `depth` é lido e gravado no projeto (`project_store.dart:374`) mas nasce e morre em `pronto`. (b) biblioteca do usuário: `effect_preset_store.dart:20` `EffectPresetStore` com JSON em disco, `add/remove/rename` — zero chamadores, e `load()` nunca é chamado no boot. Bônus: `application/ui/effect_favorites.dart:36` `effectFavoritesProvider` (estrela + chip "Favoritos", diz o comentário) — zero consumidores | Os 3 chips de `EffectPronto` cabem no topo de `_ParametrosDoEfeito` (`UI:661`), antes dos campos, chamando `applyEffectPronto`. A biblioteca de presets cabe como terceira aba dentro de `_CatalogoDeEfeitos` (`UI:898`), ao lado de "Efeitos". |
| **Parâmetro — value (number/seed)** | QUEBRADO | `UI:679-700` desenha a linha, mas `def.min`/`def.max` só viram passo (`UI:687`) e casas (`UI:691`) — **nunca limite**; `fita_de_ajuste.dart:117` `aoMudar(partida + _andado * porPixel)` sem clamp; `CTRL:4613` `editEffectParam` também não clampeia | Clampar em `editEffectParam` (`CTRL:4613`) com `spec.params[key]`. Sintoma concreto no motor: `PALCO:2982` faz `math.pow(2.0, ev)` com `ev` **sem clamp** — arrastar Exposição além do limite declarado (±3) é ganho astronômico numa `ColorFilter.matrix`. |
| **Parâmetro — value (choice / toggle)** | PASS | choice: `UI:726` (`_Escolha`, chips); toggle: `UI:711` (`_Interruptor`) | Ok. |
| **Parâmetro — keyframe** | ESCONDIDO (parcial) | o losango do rail só mira o parâmetro em `parametroAbertoProvider` (`UI:245-270`), e **só** `LinhaDeParametro` o define (`UI:693` `aoEscolher`). `_Escolha` (`UI:821`) e `_Interruptor` (`UI:1176`) não recebem `aoEscolher` — logo nenhum parâmetro de lista ou interruptor pode receber keyframe, embora `CTRL:4707` `toggleEffectParamKeyframe` aceite qualquer chave | Passar `escolhida`/`aoEscolher` para `_Escolha` e `_Interruptor` como já se faz em `LinhaDeParametro` (`UI:693`). Sem isso, animar "Canal" do Levels ou "Espelhar bordas" do Motion Tile é impossível pela UI. |
| **Parâmetro — reset** | FALTA | ver linha "Reset" | idem |
| **Parâmetro de COR (principal)** | ESCONDIDO | motor `CTRL:4726` `setEffectColor` — **zero chamadores**; a UI só **exibe** (`UI:750` `_AmostraDeCor`, três números e um quadradinho); **não existe nenhum seletor de cor em toda a `lib/`** (grep por `ColorPicker|SeletorDeCor|roda_de_cor|HueRing` → 0 arquivos). Afeta 13 efeitos com `hasColor: true` | Trocar `_AmostraDeCor` (`UI:764`) por um botão que abre um painel de cor no mesmo lugar em que `editor_de_curva.dart` já abre por cima do painel (`UI:290-295` mostra o padrão `curvaEmEdicaoProvider`). É o mesmo mecanismo: um `StateProvider` com o alvo e um painel sobreposto. |
| **Parâmetro de COR (extras)** | QUEBRADO | motor `CTRL:6548` `setEffectExtraColor` e `SPEC:2483` `withExtraColor` — zero chamadores; pior: `UI:753` só avisa sobre `ParamKind.color/point`, e `extraColors` **não é um `ParamKind`** (`SPEC:293`) — some da ficha **sem uma linha de aviso**. Atinge `gradient4` (`SPEC:2107` `extraColors: 3`) | Mesmo painel de cor, N amostras. E acrescentar `spec.extraColors` ao `semDesenho` (`UI:668`) até lá — a própria regra escrita em `UI:751-752` ("um parâmetro que some sem explicação faz o efeito inteiro parecer quebrado") está sendo violada aqui. |
| **Parâmetro de PONTO** | ESCONDIDO | `UI:736-737` joga todo `ParamKind.point` no aviso "ajuste chega numa próxima entrega"; mas o motor **renderiza**: vinheta `SPEC:1008-1009` → `PALCO:3251-3252`; raios de luz `SPEC:1104,1112` → `PALCO:3195-3196`; pixel sort `SPEC:1538,1546` → `PALCO:3493`; motion tile `SPEC:1825,1833` → `PALCO:3540` | Duas saídas, ambas já existem: (a) `AlmofadaDeArrasto` (`almofada_de_arrasto.dart`) é literalmente a superfície 2D do painel — dois `point` viram uma almofada em `_ParametrosDoEfeito` (`UI:744`); (b) alça no palco, que já tem gesto e alças (`palco_de_previa.dart`, memória "editar no palco"). O (a) é uma tarde de trabalho. |
| **Curva (easing) do keyframe de efeito** | ESCONDIDO | `UI:265-268` afirma em comentário: *"O EFEITO NÃO TEM CURVA POR TRECHO no motor de hoje"* e crava `aoAbrirCurva: null` (`UI:269`). **É falso**: `CTRL:4654` `setEffectSegmentEase(layerId, effectId, segStartLocal, ease)` e `CTRL:4682` `applyEaseToAllEffectSegments` existem, com a mesma assinatura de `setSegmentEase`/`applyEaseToAllSegments` que a UI já usa para propriedades (`UI:227-234`) | Copiar o bloco de `alvoDaPropriedade` (`UI:221-236`) para `alvoDoParametroDeEfeito` (`UI:248-270`), trocando `setSegmentEase` por `setEffectSegmentEase`. O `editor_de_curva.dart` já está pronto e já é aberto pelo mesmo provider. |
| **Keyframe universal do efeito (diamante do efeito)** | ESCONDIDO | `CTRL:4634` `toggleEffectKeyframe` (marca **todos** os parâmetros de uma vez) — zero chamadores; `SPEC:2570` `withKeyframeToggled` implementado e testado (`test/effect_universal_keyframe_test.dart`) | Um losango em `_TituloDoEfeito` (`UI:594`), ao lado do olho — é o diamante do efeito inteiro, coerente com o rail que já existe para o parâmetro. |
| **Assar movimento procedural em keyframes** | ESCONDIDO | `CTRL:755` `bakeEffectToKeyframes(layerId, effectId, fps)` — zero chamadores; `SPEC:305` `procedural` marca quais efeitos aceitam (Oscilar, Tremor…) | `_Acao` "Assar em keyframes" no fim de `_ParametrosDoEfeito` (`UI:744`), só quando `spec.procedural`. |
| **Aviso de efeito que exige GPU** | QUEBRADO | `PALCO:1665-1674`: Chroma Key, Luma Key, Color Key e Find Edges ficam **neutros e silenciosos** sem `PixelEffectEngine`, e o comentário diz "a ficha do efeito avisa que ele precisa da GPU" — a ficha nova (`UI:661-760`) não avisa nada | Acrescentar o aviso em `_ParametrosDoEfeito` (`UI:753`), na mesma `_Aviso` que já existe. |

### COLOR

| item | veredicto | prova (arquivo:linha) | onde caberia / o que fazer |
|---|---|---|---|
| **Brightness** | PASS | `SPEC:891` `curves.'brilho'` (−1..1); render `PALCO:3038`; UI genérica `UI:679-700` | Ok (via efeito **Curves**). Não existe "Brightness" avulso — quem procura o nome não acha (ver linha **Search**). |
| **Contrast** | PASS | `SPEC:2209` `corrections.'contraste'` e `SPEC:890` `curves.'contraste'`; render `PALCO:2975`, `PALCO:3037`; UI `UI:679-700` | Ok. |
| **Saturation** | PASS | `SPEC:2214` `corrections.'saturacao'`, `SPEC:909` `vibrance.'saturacao'`; render `PALCO:3052-3066` | Ok. |
| **Exposure** | QUEBRADO | `SPEC:2208` `corrections.'exposicao'` (−3..3). A UI calcula `faixa = 6` (`UI:679`) e cai em `casas: faixa <= 4 ? 3 : 0` → **zero casas decimais** (`UI:691`); `campo_de_valor.dart:29` faz `toStringAsFixed(0)`. O preset do próprio spec é `0.2` (`SPEC:2220`) e aparece como **"0"**. Render em `PALCO:2974,2982` (`pow(2, ev)`, sem clamp) | Trocar a heurística de casas por algo baseado no passo, não na faixa (`UI:691`), ou declarar `casas` por parâmetro no `EffectSpec`. Mesmo defeito em `glowVol.'exposure'` (`SPEC:466`, faixa 10 → 0 casas). Hoje Exposição é um controle que mente: você arrasta, a imagem muda, o número continua "0". |
| **Temperature** | PASS | `SPEC:934` `whiteBalance.'temperatura'` e `SPEC:2212` `corrections.'temperatura'`; render `PALCO:3069+`, `PALCO:3007+` | Ok. |
| **Tint (verde/magenta)** | PASS | `SPEC:935` `whiteBalance.'matiz'`, `SPEC:2213` `corrections.'matiz'` | Ok. |
| **Tint (efeito de tonalizar)** | QUEBRADO | `SPEC:429-442`: `hasColor: true` e **sem** `defaultColor` próprio → herda o rosa `0xFFFF5566` (`SPEC:255`). O render é literalmente `ColorFilter.mode(effect.color…)` (`PALCO:2128-2132`): a **cor é o efeito**. E a cor não tem editor (`CTRL:4726` sem chamador; `UI:750` só exibe) → **o Tint do AUREA só sabe tingir de rosa** | Igual à linha "Parâmetro de COR": seletor no lugar do `_AmostraDeCor` (`UI:764`). Enquanto não houver, dar a `tint` um `defaultColor` neutro é remendo pior — o efeito continuaria inútil. |
| **Curves (paramétrico)** | PASS | `SPEC:884-901` (contraste S, brilho, levantar sombras, baixar altas); render `PALCO:3036-3050` | Os 4 parâmetros são alcançáveis. |
| **Curves (ponto a ponto, por canal)** | FALTA | não existe no motor: `pixel_effect.dart:38` `EffectType.curves` é `PixelKernel(3, [...])` com 4 escalares; `PALCO:3043` resolve tudo em **uma** `ColorFilter.matrix`. O `editor_de_curva.dart` do projeto é editor de **easing de tempo** (`UI:290`), não de curva tonal — e o próprio guia admite: `quick_guide_screen.dart:209` "Não é o editor de curva livre ponto a ponto do After Effects" | Se entrar um dia, o widget que receberia é `_ParametrosDoEfeito` (`UI:744`) abrindo um painel sobreposto pelo mesmo padrão do `curvaEmEdicaoProvider`. **Não é um buraco de UI, é um buraco de motor** — não prometer na ficha. |
| **Levels** | PASS | `SPEC:845-883` (preto, branco, gama, saída min/max, canal); render `PALCO:2939-2974`; UI: 5 números (`UI:679`) + 1 lista (`UI:726`) | Ok, com a ressalva do keyframe: o parâmetro "Canal" é `choice` e portanto **não pode** receber keyframe (ver linha "Parâmetro — keyframe"). |
| **Color Wheels** | PASS | `SPEC:944-963` (6 números R/G/B em sombras e altas); render `PALCO:3100+` | Alcançável, mas como seis fitas numéricas. As rodas de verdade dependem do mesmo painel de cor que não existe. |
| **Vibrance** | PASS | `SPEC:902-920`; render `PALCO:3052-3066` | Ok. |
| **Gradiente 4 cores** | QUEBRADO | `SPEC:2107` `extraColors: 3` + `hasColor: true` = quatro cores; a UI mostra **uma**, só de leitura (`UI:750`), e **não avisa** sobre as outras três porque `semDesenho` (`UI:668,737`) só olha `ParamKind`; `CTRL:6548` `setEffectExtraColor` sem chamador. Sobram três fitas (opacidade, mescla, giro) para um efeito cujo assunto são as cores | Painel de cor com 4 amostras em `_ParametrosDoEfeito` (`UI:744`). Enquanto isso, no mínimo entrar no `_Aviso` (`UI:753`). |
| **Cor da camada / preenchimento** | ESCONDIDO | motor `CTRL:5819` `setShapePrimaryColor` — zero chamadores; o cartão que abriria isso está **desabilitado por decisão**: `PAINEL:156-161` `id: 'cor'`, `disponivel: false`, "Chega numa próxima entrega" | `PAINEL:156` já tem o cartão pronto; falta `_conteudo` em `UI:80-88` responder a `'cor'` e o seletor de cor existir. |
| **Cor do texto** | ESCONDIDO | motor `CTRL:5608-5612` `editTextLayer(..., Color? color)` aceita cor; a UI de texto (`UI:355-401`) expõe **só** texto, tamanho e negrito — nunca passa `color:` | Uma amostra clicável em `_Texto` (`UI:394`), ao lado do interruptor de negrito. O guia do app manda "Ajuste a cor do brilho" (`quick_guide_screen.dart:73`) — instrução impossível de cumprir hoje. |
| **Cor de fundo da composição** | ESCONDIDO | `CTRL:259` `setBackgroundColor` — zero chamadores | Cabe na categoria `'cor'` do `PAINEL:156` quando ela abrir, ou nos ajustes do projeto. |
| **Paleta do projeto (cor ligada)** | ESCONDIDO | `CTRL:858` `setPaletteColor`, `CTRL:861` `removePaletteColor`, `CTRL:865` `linkLayerColor` — sistema inteiro (trocar uma entrada muda todas as camadas ligadas, diz `CTRL:856-857`) com **zero** chamadores | Mesma categoria `'cor'` (`PAINEL:156`), como segunda seção. É funcionalidade cara que ninguém no app consegue tocar. |


---

## Frente 3

## Auditoria SHAPES + MASKS — UI nova (`lib/src/features/editor/presentation/`)

**Busca inversa feita primeiro.** A lista COMPLETA de comandos do `EditorController` que a UI nova alcanca (varredura de `presentation/` inteira) e esta:

`addAdjustmentLayer, addEffect, addElement3DLayer, addNullLayer, addParticlesLayer, addScene3DLayer, addShapeLayer, addTextLayer, applyEaseToAllSegments, beginGesture, editEffectParam, editOpacity, editShapeParam, editTextLayer, editVideoVolume, endGesture, importAudioFile, importImageFromGallery, importVideoFromGallery, moveLayer, propKeyframeTimes, removeEffect, removeLayer, reorderLayer, setSegmentEase, splitLayer, toggleEffectEnabled, toggleEffectParamKeyframe, toggleHidden, toggleKeyframe, trimLayerEnd, trimLayerStart, duplicarCamada`

De formas e mascaras, so **`addShapeLayer`** e **`editShapeParam`** entram nessa lista. **Zero** comandos de mascara. Todos os veredictos abaixo saem dai.

| item | veredicto | prova (arquivo:linha) | onde caberia / o que fazer |
|---|---|---|---|
| **SHAPES — Rectangle** | PASS | cria: `presentation/widgets/adicionar_conteudo.dart:166-170` (`ShapePresets.paramRect()`) → `application/editor_controller.dart:293` (`addShapeLayer`). edita: `presentation/widgets/controles_da_camada.dart:450-452` (`par('sizeX'/'sizeY'/'roundness')`) → `editor_controller.dart:5690` (`editShapeParam`) | Funciona, mas so 3 dos 7 controles servem a este tipo (ver QUEBRADO do painel). Cantos individuais (`cornerTopLeft..BottomLeft`, `shape.dart:358-361`) e a unidade px/% (`editor_controller.dart:5750 setShapeRoundnessUnit`) continuam sem porta |
| **SHAPES — Rounded Rectangle** | PASS | nasce com `roundness: AnimatedDouble(12)` em `domain/shape.dart:1728`; slider "Cantos" em `controles_da_camada.dart:452` | Nao ha item proprio no `+` (`adicionar_conteudo.dart:161-207`): o retangulo JA e arredondado e nao da para zerar sem arrastar. Aceitavel |
| **SHAPES — Circle/Ellipse** | PASS | `adicionar_conteudo.dart:173-177` (`paramEllipse`); render em `domain/shape.dart:490-492` (`addOval` com sx/sy); sliders `controles_da_camada.dart:450-451` | Ok. Os outros 5 sliders do painel sao inertes para elipse |
| **SHAPES — Polygon** | **QUEBRADO** | `adicionar_conteudo.dart:180-184` cria; `controles_da_camada.dart:450-451` mostra "Largura"/"Altura" como as DUAS PRIMEIRAS linhas → `editShapeParam` grava `sizeX/sizeY`, mas `domain/shape.dart:493-500` (`ParamShapeKind.polygon` → `_polystarPath`) **nunca le sizeX/sizeY**. Arrasta, o numero muda, a tela nao | `controles_da_camada.dart:450-456`: filtrar `par(...)` por `s.kind` (o `switch` de `shape.dart:487-511` diz exatamente quais chaves cada tipo usa). Hoje `shapeParamTrackOf` (`shape.dart:659-678`) responde TODA chave para TODO tipo, e por isso o `if (trilha == null) return SizedBox.shrink()` de `controles_da_camada.dart:428` nunca esconde nada |
| **SHAPES — Star** | **QUEBRADO** | mesma causa: `adicionar_conteudo.dart:187-191`; "Largura"/"Altura" inertes (`shape.dart:502-509` usa so `rOut/rIn/p/roundOut/roundIn/rotDeg`). E `outerRoundness`/`innerRoundness` (`shape.dart:376-377`), que sao o que transforma estrela em flor, nao tem linha nenhuma | idem acima; acrescentar `par('outerRoundness')`/`par('innerRoundness')` em `controles_da_camada.dart:456` quando `kind == star` |
| **SHAPES — Line** | ESCONDIDO | existe pronta: `domain/shape_library.dart:326-330` (`ShapeLibrary.line()`, bezier ABERTO + `ShapeStroke`), registrada em `shape_library.dart:40`. A lista `shapeLibrary` (`shape_library.dart:24-55`, **30 formas**) so e referenciada por `test/dnyx_remix_test.dart:48` — nenhum arquivo de `lib/` a le | `adicionar_conteudo.dart:161-207`: a familia "Formas" tem 6 itens fixos escritos a mao; a biblioteca de 30 (Linha, Cruz, Triangulo, Balao, Nuvem, X, Coracao, Check, Flor, Faisca, Cursor...) cabe inteira ali como `filhos:` de um `ItemDeAdicao`, do mesmo jeito que `_objetos3D()` faz em `adicionar_conteudo.dart:105-114` |
| **SHAPES — Custom Path** | ESCONDIDO | tres caminhos existem e nenhum e chamado: (1) desenho livre — `presentation/widgets/freehand_overlay.dart:17` esta COMPLETO (simplifica o rabisco, centraliza, cria a camada em `freehand_overlay.dart:72-80`) mas o widget nunca e montado e `freehandRequestProvider` so e posto em `false` (`editor_controller.dart:226-227`); (2) SVG — `editor_controller.dart:314` (`addSvgLayers`), 0 chamadas; (3) icone Iconify — `editor_controller.dart:354` (`addIconLayer`), 0 chamadas | `adicionar_conteudo.dart:161-207`, familia "Formas": um item "Desenhar" que so liga `freehandRequestProvider`, e montar `FreehandOverlay` no `Stack` de `editor_screen.dart:167-260` (ao lado de `PainelCentralDeAdicao`). E codigo morto pronto para usar |
| **SHAPES — Fill** | ESCONDIDO | motor: `domain/shape.dart:706-728` (`ShapeFill`), pintado em `shape.dart:1693`+ e em `palco_de_previa.dart:4294-4308`. Comando: `editor_controller.dart:5819` (`setShapePrimaryColor`) — **0 chamadas**. Na UI o cartao existe e esta DESLIGADO: `painel_da_camada.dart:156-161` (`id:'cor'`, `disponivel: false`, "Chega numa proxima entrega") | `painel_da_camada.dart:156`: virar `disponivel: true` e adicionar `'cor' => _Cor(...)` no `switch` de `controles_da_camada.dart:81-89`. Hoje a cor de uma forma e a que o preset sorteou (`shape.dart:1724-1778`) e **nao ha como muda-la** |
| **SHAPES — Stroke** | ESCONDIDO | `editor_controller.dart:6278` (`ensureShapeStroke`), `:6293` (`updateShapeStroke`), `:6303` (`removeShapeStroke`) — 0 chamadas. Cartao "Borda e sombra" desligado em `painel_da_camada.dart:163-168` | `painel_da_camada.dart:163` + `'borda' => _Borda(...)` em `controles_da_camada.dart:81-89` |
| **SHAPES — Stroke Width** | ESCONDIDO | trilha animavel `'width'` em `editor_controller.dart:6174` (`shapeItemTrack`), escrita por `:6225` (`editShapeItemTrack`) — 0 chamadas | mesma tela "Borda"; usar `LinhaDeParametro` (`controles_da_camada.dart:429-443`) com `editShapeItemTrack(id, strokeId, 'width', ...)` |
| **SHAPES — Join** | ESCONDIDO | campo `join` em `shape.dart:754`, propagado por `copyWith` (`shape.dart:785`), aplicado no `Paint` (`shape.dart:1694`) e persistido (`domain/project_store.dart:450,600-602`). So alcancavel por `updateShapeStroke` — 0 chamadas | mesma tela "Borda": um seletor de 3 estados; `_Interruptor` de `controles_da_camada.dart` e o vizinho mais proximo |
| **SHAPES — Cap** | ESCONDIDO | `shape.dart:753`, `:784`, `:1693`; `project_store.dart:449,599` | idem |
| **SHAPES — Gradient** | ESCONDIDO | `domain/shape.dart:796-890` (`ShapeGradientFill`, linear/radial, `extras`, `stops`, e **cor animavel** por `colorFrames:825`); comando `editor_controller.dart:5643` (`updateShapeGradient`) — 0 chamadas. Nenhum preset que a UI cria usa gradiente (so `ShapePresets.drop`, `shape.dart:1834`, inalcancavel) | cartao "Cor e preenchimento" (`painel_da_camada.dart:156`), como segunda aba ao lado do preenchimento solido |
| **SHAPES — Path Editing** | ESCONDIDO | `domain/path_edit.dart` inteiro (mover ancora, mover alca, inserir no sem deformar, remover, alternar canto/curva) — importado **so** por `test/path_edit_test.dart:6`. Comandos `editor_controller.dart:6011` (`convertShapeItemToBezier`), `:6061` (`editShapeBezier`), `:6345` (`ensureShapeBezierGeometry`) — 0 chamadas. E nao ha superficie: `palco_de_previa.dart` tem **0** `GestureDetector`/`Listener` na arvore inteira (4.326 linhas) | e o unico item da frente que precisa de gesto no palco. O lugar e o `Stack` de `editor_screen.dart:167-260`: uma sobreposicao irma da `FreehandOverlay`, ligada ao `CompositionView` de `editor_screen.dart:214`. O cabecalho ja sabe ser tomado por ferramenta (`editor_screen.dart:352`) |
| **SHAPES — Path Animation** | ESCONDIDO | keyframe de caminho: `editor_controller.dart:6083` (`toggleShapeBezierKeyframe`); morph: `:5988`, `:6118`, `:6139`; Trim/Drawing Progress: `:6315`, `:6382`; Repeater: `:5841`, `:6409`; operadores de caminho (offset, cantos, zigue-zague, pucker, twist, wiggle, merge): `:5860`, `:5931`. **Todos com 0 chamadas.** Os diamantes desses operadores ja estao previstos na timeline (`domain/layer.dart:967-1013`, `ShapeLayer.moduleTimesUs`) | cartao "Forma" (`painel_da_camada.dart:129-134`) precisa deixar de ser uma lista de 7 sliders e virar lista de ITENS da camada (`ShapeLayer.contents`, `layer.dart:962`), como `_Efeitos` ja faz para efeitos em `controles_da_camada.dart:494-520` — o padrao de "lista de itens + ficha do item aberto" ja existe pronto ali |
| **SHAPES — Transform** | PASS | `presentation/widgets/painel_de_transformacao.dart:124,131` (`editPosition`), `:229` (`editRotation`), `:284-288` (`editScaleUniform/X/Y`), `:183,191,260,274` (`editSkewX/Y`); aberto pelo cartao `painel_da_camada.dart:110-114` e roteado em `controles_da_camada.dart:57-76` | Ok — e a transformacao da CAMADA. A rotacao da GEOMETRIA dentro da camada (`shapeRotation`, `shape.dart:380`, chave `'shapeRotation'` em `shape.dart:673`) continua sem linha no painel |
| **SHAPES — Keyframes** | ESCONDIDO | `editor_controller.dart:5706` (`toggleShapeParamKeyframe`), `:5723` (`setShapeParamSegmentEase`), `:5736` (`applyEaseToAllShapeParamSegments`) — 0 chamadas. Na UI o rail da categoria "forma" cai no ramo generico e devolve alvo VAZIO: `controles_da_camada.dart:96-115` (`return const AlvoDoRail();` na linha 114) — diamante e curva apagados. E `editShapeParam` so vira keyframe se a trilha JA anima (`editor_controller.dart:5690-5705` → `track.edited`, `domain/keyframe.dart`), enquanto o interruptor "Keyframe automatico" (`controles_da_camada.dart:309-322`) so e lido em `editor_controller.dart:4028,4037,4115,4126` — caminho de transformacao/opacidade, **nunca** de forma. Resultado: **e impossivel animar uma forma pela UI nova** | `controles_da_camada.dart:96-115`: acrescentar um `if (categoriaId == 'forma')` que devolva `AlvoDoRail(temKeyframeAqui:..., aoAlternarKeyframe: () => c.toggleShapeParamKeyframe(...), aoAbrirCurva: ... setShapeParamSegmentEase)`, espelhando `alvoDoParametroDeEfeito` (`controles_da_camada.dart:243-266`). O `parametroAbertoProvider` que o `_Forma` ja escreve (`controles_da_camada.dart:437`) ja diz qual chave o rail deve mirar |
| **MASKS — Create Mask** | ESCONDIDO | motor completo e RENDERIZANDO: `editor_controller.dart:4903` (`addMask`), `:4911` (`applyMaskReveal` com 7 presets de revelacao, `domain/mask.dart:...createRevealMask`); composicao em `presentation/widgets/masked_box.dart:48-330` e aplicada em `palco_de_previa.dart:448-470` e `:1237-1250`; persistida em `project_store.dart:202,1160,1954`. **0 chamadas em `lib/`** | `painel_da_camada.dart:108-176` (`categoriasDaCamada`): falta o cartao `id:'mascara'` — nem desabilitado ele esta, ao contrario de "cor" e "borda". Depois, `'mascara' => _Mascaras(...)` no `switch` de `controles_da_camada.dart:81-89`. **Grave:** dois modelos de fabrica JA criam mascaras (`projects/domain/dnyx_remix_template.dart:206-209` e `projects/domain/reference_rebuild_template.dart:805-806`), ambos abertos por `projects/presentation/projects_tab.dart:17-18` — quem abre um desses tem camadas mascaradas que **nao consegue ver, editar nem remover** |
| **MASKS — Edit Points** | ESCONDIDO | `editor_controller.dart:5014` (`editMaskPath`), `:5032` (`replaceMaskPath`), `:5043` (`setMaskFromOwnShape` — "usar a propria forma como mascara"); a matematica de no esta em `domain/path_edit.dart` (so o teste importa). 0 chamadas | mesma sobreposicao de palco proposta em Path Editing (`editor_screen.dart:167-260`). `setMaskFromOwnShape` e barato e cabe como um botao dentro do painel de mascara, sem gesto nenhum |
| **MASKS — Feather** | ESCONDIDO | `editor_controller.dart:4980-5002` (`editMaskParam`, chaves `'feather'` e `'featherY'`), `:5005` (`toggleMaskFeatherAxes`, eixos ligados/soltos — `domain/mask.dart` `featherVertical`/`featherLinked`); render em `masked_box.dart` (blur montado sobre a borda). 0 chamadas | `_Mascaras` novo em `controles_da_camada.dart`: duas `LinhaDeParametro` (`controles_da_camada.dart:429-443`) + um `_Interruptor` para ligar/soltar os eixos |
| **MASKS — Expansion** | ESCONDIDO | `editor_controller.dart:4996` (`editMaskParam` chave `'expansion'`); `MaskSpec.expansion` em `masked_box.dart:18,31`; alerta de estouro de caixa ja pronto em `domain/mask.dart` (`maskFeatherExceedsBounds`). 0 chamadas | uma `LinhaDeParametro` no mesmo painel; o `maskFeatherExceedsBounds` da o aviso de graca (`_Aviso`, `controles_da_camada.dart`) |
| **MASKS — Opacity (mascara)** | ESCONDIDO | `editor_controller.dart:4997` (`editMaskParam` chave `'opacity'`); `masked_box.dart:29`. 0 chamadas | idem |
| **MASKS — Mask Modes** | ESCONDIDO | 7 modos em `domain/mask.dart` (`enum MaskMode { none, add, subtract, intersect, lighten, darken, difference }`), todos implementados no compositor (`masked_box.dart:316-322` + `:164,205-219`); comando `editor_controller.dart:4968` (`cycleMaskMode`). 0 chamadas | no cabecalho de cada mascara na lista do painel novo, como `_TituloDoEfeito` faz com o efeito (`controles_da_camada.dart:497-520`) |
| **MASKS — Invert** | ESCONDIDO | `editor_controller.dart:4975` (`toggleMaskInverted`); render em `masked_box.dart:219` (`final subtract = s.mode == MaskMode.subtract` + `inverted`). 0 chamadas | `_Interruptor` na ficha da mascara |
| **MASKS — Mask Keyframes** | ESCONDIDO (e diamante decorativo) | `editor_controller.dart:5079` (`toggleMaskPathKeyframe`); `AnimatedPath` com easing por segmento e igualacao de contagem por subdivisao (`domain/mask.dart`, `BezierPath.lerp`) — 0 chamadas. Pior: as marcas de mascara JA entram na regua (`domain/layer.dart:193-201` `maskTimesUs` → `:208-222` `keyframeTimes`), mas `layer.dart:181-187` (`podeArrastarKeyframeEm`) recusa tudo que nao seja transformacao, e a timeline so oferece `moverKeyframeDeTransformacao`/`apagarKeyframeDeTransformacao` (`linha_do_tempo.dart:283-288`). Num projeto de modelo, o diamante aparece e **nao responde a nada** | ligar o rail do painel de mascara a `toggleMaskPathKeyframe`, como em Keyframes de forma. O guarda de `layer.dart:181` esta correto e deve ficar |
| **MASKS — Multiple Masks** | ESCONDIDO | `editor_controller.dart:4903-4909` (`addMask` empilha), `:4933` (`removeMask`), `:4941` (`reorderMask`), `:4954` (`updateMask`); o compositor ja trata a pilha inteira com blend por mascara (`masked_box.dart:164-230`). 0 chamadas | lista com reordenar no painel de mascara — o padrao de arrastar ja existe em `visao_geral_das_camadas.dart:444` |
| **MASKS — Tracking de mascara** | **FALTA** | nao existe comando que ligue um rastreio a um caminho de mascara. O rastreador existe (`domain/tracker2d.dart:128` `trackSequence`, `:167` `smoothPath`) mas so alimenta estabilizacao/reenquadramento (`editor_controller.dart:2770-2830`, `autoReframeLayer`) e o blob tracker (`:2515-2560`); busca cruzada por `track`+`mask` em `editor_controller.dart` retorna zero | motor primeiro: um comando irmao de `setMaskFromOwnShape` (`editor_controller.dart:5043`) que rode `trackSequence` sobre a camada de video e grave um keyframe de caminho por quadro via `replaceMaskPath` (`:5032`). Na UI, um botao dentro do painel de mascara (que ainda nao existe). **Nao comecar por aqui**: e o unico item da frente cujo motor falta, e os 26 acima ja existem prontos |


---

## Frente 4

## RESPOSTA DIRETA À PERGUNTA DO ESTÚDIO 3D

**A UI nova NÃO chega em nenhuma das telas do estúdio 3D. Elas não existem mais.** O commit `7fe26b6` ("A UI de edicao foi apagada por inteiro, para ser refeita", 2026-09-09) apagou `presentation/am/scene3d_studio.dart` (2.493 linhas), `scene3d_sheet.dart` (3.099), `cameras_sheet.dart` (425), `scene3d_studio_ux.dart`, `rastreio3d_screen.dart`, `rastreio_sheet.dart`, `model_animation_screen.dart`, `model_import_button.dart` e `points_panel.dart`. Nada foi reconstruído. A única rota do app que ainda toca uma `Scene3D` é `Ajustes › Teste de estresse do motor 3D` (`settings_tab.dart:559` → `estresse3d_screen.dart:330`), que é uma bancada de benchmark, não um editor.

**A busca inversa é o dado central desta auditoria.** Rodei o grep de 50 comandos 3D/câmera/rastreio do `editor_controller.dart` dentro de `lib/src/features/editor/presentation/`. Resultado: **2 têm chamador** (`addElement3DLayer`, `addScene3DLayer`). **48 têm zero.** O `docs/UI_REDESIGN_PLAN.md:56` e `:318` (Q10) prometiam que "Estúdio 3D e Edit Points continuam em tela cheia, abertos por Propriedades do elemento" — a promessa não foi cumprida e a categoria "Propriedades do elemento" também não existe em `categoriasDaCamada` (`painel_da_camada.dart:112-172`).

| item | veredicto | prova (arquivo:linha) | onde caberia / o que fazer |
|---|---|---|---|
| **3D · Select** (nó dentro da cena) | FALTA | Gancho morto: `scene3d_gpu_view.dart:31` (`selectedNodeId`) nunca é passado — `palco_de_previa.dart:4067-4078` constrói `Scene3DGpuView` sem ele. O palco não tem UM gesto: `grep -c "Gesture" palco_de_previa.dart` = **0**. Não há provider de nó selecionado em lugar nenhum. | Provider de seleção + lista de nós como sub-linhas em `visao_geral_das_camadas.dart` (a pilha já sabe desenhar linha por camada). O toque no palco exige um `GestureDetector` em volta do `CompositionView` em `editor_screen.dart:216-225`. |
| **3D · Move** (nó) | ESCONDIDO | `editor_controller.dart:1794` `updateSceneNode` escreve `x/y/z` (`scene3d.dart:397-403`). Zero chamadores em `presentation/`. | Categoria nova `'cena'` em `painel_da_camada.dart:112` (só quando `camada is Scene3DLayer`) + ramo no `switch` de `controles_da_camada.dart:83-92`, reusando `AlmofadaDeArrasto`. |
| **3D · Rotate** (nó) | ESCONDIDO | `updateSceneNode` (`editor_controller.dart:1794`) sobre `rotX/rotY/rotZ` (`scene3d.dart:401-407`). Zero chamadores. | Mesmo ramo; `dial_de_angulo.dart` já é a superfície certa, um dial por eixo. |
| **3D · Scale** (nó) | ESCONDIDO | `updateSceneNode` sobre `scale` (`scene3d.dart:404`) + `size` (`:406`). Zero chamadores. | Mesmo ramo; `fita_de_ajuste.dart`. |
| **3D · Gizmo** | ESCONDIDO | `AxisGizmo` existe em `scene3d_painter.dart:1365` e **não é instanciado por ninguém em lib/ nem em test/** — órfão do estúdio apagado. O `NullGizmoPainter` (`particles_painter.dart:511`) é o único gizmo vivo, e só para `NullLayer` (`palco_de_previa.dart:4173`). | `AxisGizmo` volta como sobreposição do palco quando há nó selecionado, dentro do `Stack` de `editor_screen.dart:172-250`. |
| **3D · Orbit** (navegar a vista) | ESCONDIDO | Rig existe: `addOrbitRig` (`editor_controller.dart:1649`) cria nulo + parenteia a câmera + keyframes reais. `alignCameraToRender` (`:2146`) aceita câmera livre. Zero chamadores dos dois. Navegação por dedo não existe (palco sem gesto). | `addOrbitRig` cabe como ação de um toque na categoria da cena (`controles_da_camada.dart:1059` `_AcoesDaCamada` é o modelo de "o que se faz COM a camada"). |
| **3D · Pan** (navegar a vista) | FALTA | Não há comando de deslocar a vista livre; era estado interno do `scene3d_studio.dart` apagado. O que existe é `alignCameraToRender` (`:2146`), que só recebe uma câmera pronta. | Só volta junto com um gesto no palco; sem isso não há onde encaixar. |
| **3D · Zoom** | ESCONDIDO | `Camera3D.focalLength` (`camera3d.dart:365`), `fovAt` (`:383`), editável por `updateScene3DCamera` (`editor_controller.dart:1411`). Zero chamadores. | Linha de parâmetro "Distância focal" na categoria da cena (`linha_de_parametro.dart` + `campo_de_valor.dart`). |
| **3D · Focus Selected** | ESCONDIDO | `focusCameraOnNode` (`editor_controller.dart:1968`, ajusta o DOF pela distância real do objeto) e `frameSceneNode` (`:1992`). Zero chamadores. | Ação rápida na categoria da cena, ao lado da lista de nós. |
| **3D · Vistas Front/Back/Left/Right/Top/Bottom** | ESCONDIDO | `enum SceneView` completo (`camera3d.dart:529-539`), rótulos em `:541-549`, `orthoViewCamera` (`:556`), `setScene3DView` (`editor_controller.dart:1771`), `alignCameraToCurrentView` (`:2156`). O palco lê `l.view` (`palco_de_previa.dart:4071`) mas ele fica preso em `SceneView.camera` (padrão em `layer.dart:2398`) para sempre. | Um `PilulaDeNavegacao` (`pilula_de_navegacao.dart`) de 7 posições no topo da categoria da cena. |
| **3D · Perspective** | PASS (por omissão, sem controle) | Renderiza: `palco_de_previa.dart:4067-4078` → `Scene3DGpuView`; a projeção sai de `Camera3D.fovAt` (`camera3d.dart:383`) com `orthographic=false` (`:323`). Mas é o padrão imutável — não existe escolha. | O par com Ortográfica, num interruptor na categoria da cena. |
| **3D · Orthographic** | ESCONDIDO | Campo existe e o motor honra: `camera3d.dart:367`, `scene3d_gpu.dart:346`, `camera_ortografica.dart:62`, serializado em `project_store.dart:1835/1875`. Só alcançável por `updateScene3DCamera(id,(c)=>c.copyWith(orthographic:…))` (`editor_controller.dart:1411` + `camera3d.dart:486`) — e **não há comando dedicado nem chamador**. | Interruptor "Ortográfica" na categoria da cena; e vale criar o `setCameraOrthographic` no controller, porque hoje o único caminho é um `copyWith` cru. |
| **3D · Troca de câmera** | ESCONDIDO | `setCameraShot` (`editor_controller.dart:1477`, com tolerância de 60 ms para trocar a tomada em vez de empilhar), `removeCameraShot` (`:1500`), `clearCameraShots` (`:1514`); o motor resolve em `layer.dart:2461` e interpola em `camera_cuts.dart:66`. Zero chamadores. | A régua de `linha_do_tempo.dart` já desenha marcas; o corte de câmera é uma marca na régua + um chip de câmera no `_Transporte` (`linha_do_tempo.dart:323`). |
| **3D · Create Camera** | ESCONDIDO | `addScene3DCamera` (`editor_controller.dart:1436`) — nasce com o enquadramento atual, de propósito. Zero chamadores. | Botão "+" da lista de câmeras, na categoria da cena. |
| **3D · Presets de câmera** (rigs) | ESCONDIDO | `enum CameraRig {orbit,tripod,dolly,handheld,dollyZoom}` (`camera3d.dart:775`), rótulos em PT (`:777-783`), `applyCameraRig` (`:786-860`), exposto por `applyRigToScene` (`editor_controller.dart:1837`) — gera keyframes REAIS. Zero chamadores. | Cinco cartões numa fileira dentro da categoria da cena; é o item de maior retorno por menos código de todos os 48. |
| **3D · Lights** | ESCONDIDO | `addSceneLight` (`editor_controller.dart:1869`), `updateSceneLight` (`:1881`), `removeSceneLight` (`:1894`); `Light3DKind {directional,point,ambient,spot}` (`scene3d.dart:246`). Zero chamadores. A cena nova já nasce com três pontos (`editor_controller.dart:1396`), então não fica preta — mas é imutável. | Lista de luzes na categoria da cena; `adicionar_conteudo.dart:116-139` (família "3D") também deveria ter "Luz", como o próprio plano previa (`UI_REDESIGN_PLAN.md:107`). |
| **3D · Materials** | ESCONDIDO | `Material3D` (`scene3d.dart:27`), **12 presets nomeados em PT** (`:133-161`, `materialFromPreset` `:163-244`), aplicáveis por `updateSceneNode` (`editor_controller.dart:1794`). Zero chamadores. | Grade de 12 miniaturas na categoria do nó — o mesmo padrão que `_Efeitos` já usa (`controles_da_camada.dart:492`). |
| **3D · Environment** | ESCONDIDO | `Scene3D.environment` (`scene3d.dart:664/679`), `envReflect` (`:680`), `panorama` (`:681`), `reflectionProbe` (`:682`), névoa (`:688-690`), tudo por `updateScene3D` (`editor_controller.dart:1405`). Zero chamadores em `presentation/` (o único uso do app inteiro é o benchmark: `estresse3d_screen.dart:330`). | Sub-aba "Ambiente" na categoria da cena. **Nota de custo:** medido, o ambiente é 92% do quadro travado — o controle precisa nascer junto com o crachá de qualidade. |
| **3D · 3D Transform** (da camada) | **QUEBRADO** | O campo `z` aparece sempre no painel, mas só aceita dedo quando `l.is3D` (`painel_de_transformacao.dart:129-143`) — e **`toggle3D` (`editor_controller.dart:6566`) não tem nenhum chamador**. Logo o campo é permanentemente inerte para texto, forma, imagem, vídeo, legenda e ajuste. Pior: `editRotationX` (`:4141`) e `editRotationY` (`:4144`) existem e **nenhuma superfície os chama** — o modo "girar" é um dial só, de Z (`painel_de_transformacao.dart:225-230`). | O `RailDireito` (`painel_de_transformacao.dart:92-99`) já tem 4 modos; "girar" precisa de 3 dials (X/Y/Z) quando `is3D`, e a categoria "Camada" (`controles_da_camada.dart:1059`) precisa do interruptor "Ligar 3D" — exatamente o que `UI_REDESIGN_PLAN.md:134` mandava. |
| **3D · Parent** | ESCONDIDO | Duas pontas, nenhuma chamada: camada→camada por `linkProperty(..., LayerProp.parent, ...)` (`editor_controller.dart:6724`, caso em `:6750-6773`, com captura do transform efetivo para nada pular); nó→nó por `setSceneNodeParent` (`:1583`, com recusa de ciclo em `:1606`). O palco já resolve a cadeia inteira (`palco_de_previa.dart:1098`). | A alça de reordenar da pilha (`visao_geral_das_camadas.dart`) é o lugar natural do pickwhip: arrastar uma camada sobre outra = parentear. Sem isto o "Nulo 3D" do `+` é decoração. |
| **3D · Null** | PASS (só criar) / ESCONDIDO (usar) | Cria: `adicionar_conteudo.dart:127-132` → `addNullLayer` (`editor_controller.dart:1319`, com `is3D:true`), e o palco desenha o gizmo só no editor (`palco_de_previa.dart:4169-4173`). **Mas nada pode ser pendurado nele** (ver Parent). O nulo interno da cena, `addSceneNull` (`editor_controller.dart:1567`), não tem chamador nenhum. | O `+` já está certo; o que falta é o parenting (linha acima). |
| **3D · Model Import** | ESCONDIDO | Leitor completo e testado: `readModel3DFiles` (`model_import_service.dart:11`, GLB/glTF/OBJ/FBX com recursos vizinhos); entrada no projeto por `addModel3D` (`editor_controller.dart:1714`) e `addGlbNode` (`:1675`). **Nenhum seletor de arquivo de modelo na UI** — os únicos `FilePicker` do app são vídeo (`autoedit_screen.dart:37`), mídia (`media_import_service.dart:83`) e `.json/.aurea/.xml` (`projects_tab.dart:199/230`). O único jeito de um modelo entrar hoje é o template Monolito (`projects_tab.dart:136`), que traz três modelos empacotados. | Item "Importar modelo" na família "3D" do `+` (`adicionar_conteudo.dart:116-139`) — o `ItemDeAdicao.criar` já é `FutureOr`, feito exatamente para seletor de sistema (`:44`). |
| **3D · Model Hierarchy** | ESCONDIDO | O dado é lido e guardado: `ModelSource3D.nodeNames/animationNames/meshes/materials/textures` (`scene3d.dart:361-390`, preenchido em `editor_controller.dart:1690-1712` e `:1721-1738`). Nenhuma tela lê. | Ficha de leitura na categoria do nó, no molde de `_Midia` (`controles_da_camada.dart:991`). |
| **3D · Animation** (clipes do modelo) | ESCONDIDO | `ModelMotion3D {clip,speed,offset,loop,keys}` (`model_asset3d.dart:487-498`) é campo do `SceneNode` (`scene3d.dart:423`) e é avaliado no render; só mudaria por `updateSceneNode` (`editor_controller.dart:1794`). A tela dedicada foi apagada (`am/model_animation_screen.dart`, commit `7fe26b6`) e o gancho de pose ficou órfão (`scene3d_gpu_view.dart:30` `showModelRig`, nunca passado). | Sub-ficha "Animação" na categoria do nó: seletor de clipe (os nomes já vêm em `ModelSource3D.animationNames`), velocidade, offset, laço. |
| **3D · 3D Keyframes** | **QUEBRADO** | `toggleKeyframe` grava **três eixos de rotação de uma vez** (`editor_controller.dart:4389-4398`) e Z junto com posição (`:4370-4384`), e o rail chama isso de verdade (`controles_da_camada.dart:225`, `painel_de_transformacao.dart:66`). Só que não existe superfície para mudar rotX/rotY (ver 3D Transform): a pessoa marca keyframes em trilhas que nunca vai conseguir mover, e elas passam a existir no arquivo. Para nós e câmeras da cena, os `AnimatedDouble` existem (`scene3d.dart:401-407`, `camera3d.dart:344-365`) e nada na UI os marca. | Consertar junto com o modo "girar" de `painel_de_transformacao.dart`; e o rail (`rails_do_painel.dart`) precisa aceitar alvo de propriedade de nó/câmera, não só `LayerProp` (`video_project.dart:12`, que nem tem `positionZ`/`rotationX`). |
| **3D · 3D Motion Graph** | FALTA | `editor_de_curva.dart` é editor de **easing por trecho** (`:14-33`, presets em `:38-46`), não gráfico de valor/velocidade; e só é aberto por `alvoDaPropriedade`/`alvoDoParametroDeEfeito` (`controles_da_camada.dart:187/245`), que não conhecem cena 3D. Não existe gráfico de valor no motor. | Nada a "esconder": é trabalho novo. Se entrar, entra como segundo modo do `editor_de_curva.dart`, que já tem o painel e o cabeçalho tomado. |
| **CÂM · Camera 1/2/3** (lista) | ESCONDIDO | O modelo é multi-câmera: `allCameras` (`layer.dart:2445`), `extraCameras` (`:2432`), `shots` (`:2435`), resolução com corte em `:2461-2466`. Nenhuma lista na UI. | Lista de câmeras na categoria da cena — é a peça que destrava New/Rename/Duplicate/Delete/Activate de uma vez. |
| **CÂM · New** | ESCONDIDO | `addScene3DCamera` (`editor_controller.dart:1436`). Zero chamadores. | `+` da lista de câmeras. |
| **CÂM · Rename** | ESCONDIDO | `renameSceneCamera` (`editor_controller.dart:2040`). Zero chamadores. | Toque longo / campo na linha da câmera. |
| **CÂM · Duplicate** | ESCONDIDO | `duplicateSceneCamera` (`editor_controller.dart:2049`, copia inclusive DOF e lookAt). Zero chamadores. | Ação na linha da câmera. |
| **CÂM · Delete** | ESCONDIDO | `removeScene3DCamera` (`editor_controller.dart:1457`, e ainda limpa as tomadas órfãs em `:1466-1471`). Zero chamadores. | Ação destrutiva na linha da câmera. |
| **CÂM · Activate** (cortar para) | ESCONDIDO | `setCameraShot` (`editor_controller.dart:1477`). Zero chamadores. | Toque na câmera da lista = corta no cabeçote; a marca aparece na régua de `linha_do_tempo.dart`. |
| **CÂM · Focus** | ESCONDIDO | `frameSceneAll` (`editor_controller.dart:1855`), `frameSceneNode` (`:1992`), `focusCameraOnNode` (`:1968`), `applySavedView` (`:2130`), `saveSceneView` (`:2105`), `removeSceneView` (`:2117`). Seis comandos, zero chamadores. | "Enquadrar tudo" como ação rápida; vistas salvas como fileira de chips. |
| **CÂM · Animação de câmera** | ESCONDIDO | A câmera é animável por construção: `posX/posY/posZ/poiX/poiY/poiZ/focalLength` são `AnimatedDouble` (`camera3d.dart:344-365`); escrita por `updateScene3DCamera` (`editor_controller.dart:1411`) e `updateSceneCameraById` (`:1417`). Zero chamadores. `applyRigToScene` (`:1837`) já geraria keyframes editáveis. | Categoria da cena › Câmera, com as mesmas `LinhaDeParametro` do resto do painel. |
| **CÂM · FOV** | ESCONDIDO | `focalLength` (`camera3d.dart:365`), `fovAt` (`:383`), `zoomFromFocal` (`:390`), `filmWidth` (`:366`). Zero chamadores. | Linha de parâmetro (mm) + leitura do ângulo em graus. |
| **CÂM · Position** | ESCONDIDO | `posX/posY/posZ` + `poiX/poiY/poiZ` (`camera3d.dart:344-360`), por `updateScene3DCamera` (`editor_controller.dart:1411`). Zero chamadores. | Três `CampoDeValor` (`campo_de_valor.dart`) + `AlmofadaDeArrasto`. |
| **CÂM · Rotation** | ESCONDIDO | `orientX/Y/Z`, `rotX/Y/Z`, `autoOrient`, `lookAtNodeId` (`camera3d.dart:484-509`); `setCameraLookAt` (`editor_controller.dart:2081`). Zero chamadores. | Três dials + interruptor "Olhar para", com o alvo saindo da lista de nós. |
| **CÂM · Quão rápido é trocar** | ESCONDIDO | Custo **infinito**: não existe superfície de troca. `resolveCamera` (`layer.dart:2461`) e a interpolação de corte (`camera_cuts.dart:66`) estão prontos e mortos. | Com a lista de câmeras proposta, a troca vira **um toque**; com o corte na régua, vira um toque + um arrasto. |
| **RAST · Import Video** | PASS | UI: `adicionar_conteudo.dart:226-232` → `importVideoFromGallery`; comando: `editor_controller.dart` (`importVideoFromGallery`, seletor em `media_import_service.dart:83`). O vídeo entra e toca (`video_layer_manager.dart` via `editor_screen.dart:56-64`). | — |
| **RAST · Track** | ESCONDIDO | `rastrearCamera3D` (`editor_controller.dart:2644`) → `CameraTrackService.instance.rastrear` (`camera_track_service.dart:27/105`), com `ModoDoSolve` e `TipoDeTomada` (`camera_solver3d.dart:377/332`). Zero chamadores em `presentation/`. | Categoria "Rastrear" para `VideoLayer` em `painel_da_camada.dart:112` (hoje o vídeo só ganha `volume` e `midia`, `:136-145`) + ramo em `controles_da_camada.dart:83-92`. |
| **RAST · Solve** | ESCONDIDO | Solver inteiro vivo e testado (`camera_solver3d.dart:472-670`: essencial, homografia, SVD; `test/camera_solver3d_test.dart`, `test/camera_tracker_pro_test.dart`), acessível só por `rastrearCamera3D` (`editor_controller.dart:2644`), que não tem chamador. Inclui a **recusa honesta** de tripé/cena plana (`LeituraDaCena`, `camera_solver3d.dart:353`) que ninguém nunca vê. | Mesma categoria "Rastrear": botão + ficha do solve (erro em px, quadros, pontos), no molde de `_Midia` (`controles_da_camada.dart:991`). |
| **RAST · Track Points** | ESCONDIDO | `SolucaoCamera3D.nuvem` (`camera_solver3d.dart:114`), `errosPorPonto` (`:130`), `vistasPorPonto` (`:134`), `QualidadeDoPonto` (`:311`); `nuvemDoRastreio` (`cena_do_rastreio.dart:125`) já vira nó da cena via `criarCenaDoRastreio(comNuvem:true)` (`editor_controller.dart:2682/2694`). Nada disso é desenhado hoje — o `_PintorDosPontos` morreu com `rastreio3d_screen.dart:858`. | Sobreposição sobre a prévia dentro da categoria "Rastrear" (o `Stack` de `editor_screen.dart:172-250` é o lugar). |
| **RAST · Select Points** | FALTA | Era estado de tela (`_escolhidos` em `rastreio3d_screen.dart:199/223`), apagado com ela. Não existe comando no controller nem no domínio. | Só volta junto com o desenho da nuvem; sem a nuvem na tela não há o que selecionar. |
| **RAST · Ground Plane** | ESCONDIDO | Três funções completas e **sem um único chamador em lib/**: `planoDosPontos` (`plano_do_rastreio.dart:129`), `maiorPlano` (`:237`), `TipoDeSuperficie` (`:109`), e `noNoPlano` (`cena_do_rastreio.dart:232`). Eram chamadas por `rastreio3d_screen.dart:133/199/349`. | Botão "Achar o chão" na categoria "Rastrear", com o automático (`maiorPlano`) como padrão. |
| **RAST · Create Camera** (do rastreio) | ESCONDIDO | `criarCenaDoRastreio` (`editor_controller.dart:2682`) — cria a `Scene3DLayer` com a câmera resolvida e a insere **acima do clipe**, não no topo (`:2695-2700`); `camadaDoRastreio` (`cena_do_rastreio.dart:168`), `cameraDoRastreio` (`:52`). Zero chamadores. | O passo 2 da categoria "Rastrear", depois da ficha do solve. |
| **RAST · Create Null** | ESCONDIDO | `criarNoDoPonto` (`editor_controller.dart:2706`) → `noNoPonto` (`cena_do_rastreio.dart:147`). Zero chamadores. | Toque num ponto da nuvem desenhada = nulo ali. |
| **RAST · Create 3D Object** | ESCONDIDO | `noNoPlano` (`cena_do_rastreio.dart:232`) + `enum ObjetoNoPlano` (`:200`) — plano, caixa, texto sobre o plano rastreado. **Nem comando no controller existe** (nunca teve: a tela chamava o domínio direto, `rastreio3d_screen.dart:349`), e agora ninguém chama. | Precisa de um comando no controller (`criarObjetoNoPlano`) + fileira de tipos na categoria "Rastrear". |
| **RAST · Re-solve** | ESCONDIDO | `semPontos` (`plano_do_rastreio.dart:315`) tira pontos e devolve a solução para resolver de novo; era o "limpar ruins" de `rastreio3d_screen.dart:223/235`. Zero chamadores. | Duas ações na ficha do solve: "tirar os escolhidos" e "tirar os ruins", com o erro em px atualizando na hora. |

**Placar:** 2 PASS · 3 QUEBRADO · 39 ESCONDIDO · 4 FALTA · 0 N/A.

---

### OS TRÊS ACHADOS MAIS GRAVES

**1. O estúdio 3D não foi "refeito depois": ele foi apagado e nada ocupou o lugar.** Seis mil linhas de UI 3D saíram no commit `7fe26b6`, e a busca inversa mostra o tamanho do buraco: dos 50 comandos 3D/câmera/rastreio do `editor_controller.dart`, **48 não têm um único chamador em `presentation/`** — incluindo os 12 comandos de câmera, os 3 de luz, os 6 de enquadramento e os 3 de rastreio. Isso não é "falta de porta": é o motor inteiro do 3D pendurado no nada, sustentado só por testes de unidade. O `docs/UI_REDESIGN_PLAN.md:56` prometia o estúdio em tela cheia aberto por "Propriedades do elemento", e nem essa categoria existe em `categoriasDaCamada` (`painel_da_camada.dart:112-172`).

**2. O `+` oferece "Cena 3D" e entrega uma armadilha.** `adicionar_conteudo.dart:121-126` cria a camada por `addScene3DLayer` (`editor_controller.dart:1390`), que nasce **sem nenhum nó** — e os três comandos que povoariam a cena (`addSceneNode:1778`, `addModel3D:1714`, `addSceneLight:1869`) não têm chamador. Selecionar essa camada dá as mesmas quatro categorias de uma imagem (`painel_da_camada.dart:112-172`: transformar, opacidade, camada, efeitos). O resultado é uma camada que mostra só a grade de ajuda — e nem a grade dá para desligar, porque `showHelpers` nasce `true` (`layer.dart:2399`) e `setScene3DHelpers` (`editor_controller.dart:1765`) também não tem chamador. O arquivo do comentário em `adicionar_conteudo.dart:23-25` diz "Nada de item aceso que abre o nada"; este item abre o nada.

**3. O rastreio de câmera 3D — o recurso mais caro que o app já construiu — está 100% inalcançável, e o Z do painel mente.** Solver, serviço, detecção de plano, nuvem de pontos, recusa de tripé, tudo vivo e testado (`camera_solver3d.dart`, `plano_do_rastreio.dart`, `cena_do_rastreio.dart`, `camera_track_service.dart`) e sem um botão; `maiorPlano`, `planoDosPontos`, `noNoPlano` e `semPontos` não têm chamador em **lib/ inteira**. No mesmo tema, o único pedaço de 3D que a UI nova realmente toca está quebrado: o campo `z` fica visível e apagado para sempre (`painel_de_transformacao.dart:129-143`) porque `toggle3D` (`editor_controller.dart:6566`) não tem chamador, e o botão de keyframe de rotação grava trilhas em X e Y (`editor_controller.dart:4389-4398`) que nenhuma superfície do app consegue mover. É pior que ausência: é o app escrevendo no projeto do usuário uma animação que ele não pediu, não vê e não pode editar.

---

## Frente 5

## KEYFRAMES

| item | veredicto | prova (arquivo:linha) | onde caberia / o que fazer |
|---|---|---|---|
| **Criar keyframe** | **QUEBRADO** | motor `toggleKeyframe` `editor_controller.dart:4357`; UI `controles_da_camada.dart:225` → `rails_do_painel.dart:77`. O DEFEITO: a UI decide "tem marca aqui?" por igualdade EXATA de microssegundo (`controles_da_camada.dart:198`, `locais.contains(local)`), o motor decide por tolerancia de 8 ms (`keyframe.dart:478` + `keyframe.dart:287`) | Com o cabecote a menos de 8 ms de uma marca, o losango fica VAZADO ("Marcar keyframe aqui") e o toque APAGA a marca. Alcancavel so pausando a reproducao ou arrastando a regua. Correcao de uma linha: `controles_da_camada.dart:198` passa a usar `hasKeyframeAt` da propria trilha, como o caminho de efeito ja faz em `controles_da_camada.dart:261` |
| **Remover** | PASS | mesmo losango (`editor_controller.dart:4362-4367`) e toque longo na trilha: `linha_do_tempo.dart:908-914` → `apagarKeyframeDeTransformacao` `editor_controller.dart:4333`. Ressalva dura: `tirar()` usa `withoutKeyframe` (`keyframe.dart:321`, ±8 ms), entao apagar um losango apaga tambem qualquer marca vizinha dentro de 8 ms que a timeline desenhou como losango SEPARADO (`linha_do_tempo.dart:1128`, `keyframeTimes` e exato) | Funciona; so falta alinhar a tolerancia. E o toque longo apaga sem confirmacao nem aviso — no aparelho isso e uma perda silenciosa |
| **Mover no tempo** | **QUEBRADO** | UI `linha_do_tempo.dart:283-285` e `_mover` `linha_do_tempo.dart:860-885` → `moverKeyframeDeTransformacao` `editor_controller.dart:4296`. Tres defeitos: (a) o arrasto NAO abre lote de desfazer — nao ha `beginGesture/endGesture` em `linha_do_tempo.dart` (grep: so `controles_da_camada`, `painel_de_transformacao:72`, `visao_geral:263`), entao a janela de 450 ms de `editor_controller.dart:118` racha o arrasto em N passos; (b) a guarda de colisao e exata (`editor_controller.dart:4306`) mas `_insertSorted` come tudo em ±8 ms (`keyframe.dart:312`), entao soltar perto de outra marca a apaga em silencio (a 480 px/s, `mapa_do_tempo.dart:98`, 8 ms = 3,8 px); (c) quando o motor RECUSA o movimento, a UI ainda faz `_pego = destino` (`linha_do_tempo.dart:877`) e passa a apontar para um instante sem marca — os movimentos seguintes viram no-op e `_soltar` (`linha_do_tempo.dart:892`) leva o cabecote para o vazio | Embrulhar `_comecarArrasto`/`_soltar` em `beginGesture`/`endGesture`; fazer `moverKeyframeDeTransformacao` devolver bool e so entao atualizar `_pego`; trocar a guarda exata de `4306` por uma comparacao com o mesmo epsilon |
| **Alterar valor** | PASS | `AnimatedDouble.edited` `keyframe.dart:621` ← `_editDouble` `editor_controller.dart:4020` ← `editOpacity`/`editPosition`/`editScale*`; UI `controles_da_camada.dart:293` (`LinhaDeParametro`) e `painel_de_transformacao.dart:123-131` (campos x/y/z) | Funciona. Ressalva: editar com o cabecote 3 ms fora da marca reescreve a marca no tempo NOVO (`keyframe.dart:312`) — o valor muda e a marca anda junto |
| **Selecionar varios** | **FALTA** | nao existe modelo de selecao de keyframe em lugar nenhum: o estado da trilha guarda UM (`linha_do_tempo.dart:777` `_pego`, `:792` `_candidato`); nenhum provider de selecao de marcas em `presentation/` | Cabe em `_FaixaState` (`linha_do_tempo.dart:774`): toque longo entra no modo de selecao, toque acumula. O rail esquerdo (`rails_do_painel.dart:58-88`) tem so 3 botoes numa coluna de 300 px e sobra lugar para o "n marcas" |
| **Mover varios** | **FALTA** | consequencia direta da linha acima. O que existe no motor e outra coisa: `cascadeSelection` `editor_controller.dart:6586` e `linkCascadeSelection` `:6610` escalonam keyframes de VARIAS CAMADAS (stagger), e nao varias marcas — e tem ZERO chamadores (grep em `lib/`, so a definicao) | Depois da selecao multipla, `moverKeyframeDeTransformacao` ja e a peca certa: basta chama-la em laco dentro de `runAsOneUndo` (`editor_controller.dart:152`). A cascata cabe em `_AcoesDaCamada` (`controles_da_camada.dart:90`) |
| **Duplicar** | **FALTA** | `duplicated()` so existe para camada (`layer.dart:252`) e efeito (`effect.dart:2609`); nada duplica keyframe. Nenhum comando de keyframe em `editor_controller.dart` cria copia | Quarto botao no `RailEsquerdo` (`rails_do_painel.dart:58`), ou toque longo no losango em `_FaixaState` (`linha_do_tempo.dart:908`, hoje ocupado pelo apagar) |
| **Copiar** | **FALTA** | zero area de transferencia no motor: grep por `clipboard/copiar/paste` em `editor_controller.dart` so devolve `copyWith`/`copyLayer` | Precisa nascer no motor (um `Map<LayerProp, List<Keyframe>>` de sessao) antes de ter porta. A porta seria o rail esquerdo |
| **Colar** | **FALTA** | idem | idem |
| **Interpolacao Linear** | PASS | preset `editor_de_curva.dart:39` → `aplicar` `:89-92` → `setSegmentEase` `editor_controller.dart:4412`; ligado em `controles_da_camada.dart:231` | — |
| **Hold** | **FALTA** | `EasingType` nao tem hold (`keyframe.dart:9-22`). O parente mais proximo, `steps`, e uma escada: `((t*n).floor()/(n-1))` `keyframe.dart:113-115` — com o `count` padrao 4 (`keyframe.dart:29`) da 0, 1/3, 2/3, 1. Prova de que isso ja mordeu: `cena_xml_import.dart:418` mapeia `'constant'` (hold) para `EasingType.steps`, ou seja, um hold importado toca como escada de quatro degraus | Um `EasingType.hold` com `transform(t) => 0` no `switch` de `keyframe.dart:98`, mais um par em `presetsDeCurva` (`editor_de_curva.dart:38-46`) — a miniatura do rail (`editor_de_curva.dart:313`) ja desenha sozinha |
| **Ease In** | PASS | `Easing.easeIn` `keyframe.dart:38` → preset "Entra" `editor_de_curva.dart:41` → `setSegmentEase` `editor_controller.dart:4412` | — |
| **Ease Out** | PASS | `keyframe.dart:39` → "Sai" `editor_de_curva.dart:42` → idem | — |
| **Ease In/Out** | PASS | `keyframe.dart:40` → "Suave" `editor_de_curva.dart:40` → idem | — |
| **Bezier** | **QUEBRADO** | as duas alcas movem os proprios `x1,y1,x2,y2` do motor: `_QuadroState.mover` `editor_de_curva.dart:404-412`, gesto em `:416-427`, desenho em `_PintorDaCurva` `:464`. O DEFEITO: `aplicar` (`editor_de_curva.dart:89`) escreve a cada `onPanUpdate` e NAO ha `beginGesture/endGesture` no arquivo inteiro — e exatamente a falha que o proprio controller documenta como razao de existir o lote: "arrastar uma alca de curva e depois desfazer devolvia so metade do movimento" (`editor_controller.dart:172-177`) | Chamar `beginGesture` no `onPanStart` (`editor_de_curva.dart:416`) e `endGesture` no `onPanEnd` (`:427`). O `CurvaEmEdicao` (`editor_de_curva.dart:16`) ja carrega callbacks; bastam mais dois |
| **Keyframe ativo identificado** | PASS | timeline: `_pintarKeyframes` pinta o losango sob o cabecote com o acento e contorno (`linha_do_tempo.dart:1149-1159`, tolerancia de 6 px em `:1137`); rail: `aceso` (`rails_do_painel.dart:71-78`, cor em `:193-196`) | Funciona. Incoerencia visual: o rail desenha `Icons.change_history_rounded` (`rails_do_painel.dart:72`), que e um TRIANGULO, enquanto o comentario logo acima diz "O LOSANGO E O SIMBOLO DE KEYFRAME" e a timeline desenha losango de verdade (`linha_do_tempo.dart:1152-1157`). Dois simbolos para a mesma coisa |
| **Indicador de propriedade animada** | PASS (parcial, e o parcial doi) | `AlvoDoRail.animado` `rails_do_painel.dart:25`, alimentado por `locais.isNotEmpty` `controles_da_camada.dart:224` e `trilha.isAnimated` `:262`, pintado como meio-aceso `rails_do_painel.dart:79/85/195` | So vale para a propriedade que o rail esta MIRANDO. Num efeito com doze parametros (`controles_da_camada.dart:671`) e preciso tocar em cada chip e olhar o rail para descobrir quais animam: `LinhaDeParametro` nao tem nenhum estado de keyframe (`linha_de_parametro.dart:25-64`). Cabe um ponto no `_Chip` (`linha_de_parametro.dart:71`), com o dado que `efeito.params[chave].isAnimated` ja da de graca |
| **Proximo keyframe** | **FALTA** | nenhum comando: grep por `nextKeyframe/proximoKeyframe/irParaKeyframe` em `lib/` = 0. O transporte tem SETE alvos e nenhum e marca — desfazer, refazer, inicio, play, fim, duplicar, enquadrar (`linha_do_tempo.dart:312-322`). O grupo de teste que se chama "navegar entre marcas" (`test/efeitos_e_curva_test.dart:190`) na verdade so testa o rail acender. Substituto acidental: tocar num losango leva o cabecote ate ele (`linha_do_tempo.dart:832-844`), e so no modo detalhado | **O dado ja esta calculado e jogado fora**: `alvoDaPropriedade` acha a marca anterior e a proxima em `controles_da_camada.dart:200-205` (`inicioDoTrecho`, `depois`) e usa isso so para decidir se o botao de curva acende. Poe `‹◆` e `◆›` ao redor do losango no `RailEsquerdo` (`rails_do_painel.dart:66-80`) e chama `playback.seek` — a coluna ja e um `Column` com `Expanded` e aceita mais dois |
| **Keyframe anterior** | **FALTA** | idem acima | idem acima |
| **Auto Keyframe** | **ESCONDIDO** | o provider e GLOBAL e manda em posicao, escala e rotacao (`editor_controller.dart:60`, lido em `:4028`, `:4037`, `:4115`, `:4126`). O interruptor so e desenhado dentro de `_Opacidade` (`controles_da_camada.dart:297`, classe em `:309`). `PainelDeTransformacao` nao o renderiza em lugar nenhum (`painel_de_transformacao.dart:79-107`) | O interruptor que decide se ARRASTAR A CAMADA cria keyframe mora atras da ferramenta "Opacidade". Sobe para `ControlesDaCategoria._ComRail` (`controles_da_camada.dart:134`) para valer em toda ferramenta, ou pelo menos entra na fileira de campos do `PainelDeTransformacao` (`painel_de_transformacao.dart:88`) |

## MOTION GRAPH

| item | veredicto | prova (arquivo:linha) | onde caberia / o que fazer |
|---|---|---|---|
| **Value Graph** | **FALTA** | o que existe nao e um grafico de valor: `EditorDeCurva` recebe apenas titulo + `Easing` (`editor_de_curva.dart:16-33`) — nunca ve a trilha. O pintor percorre `curva.transform(t)` num quadrado 0..1 (`editor_de_curva.dart:464-494`), e o quadro e o `LayoutBuilder` inteiro sem transformacao (`:389-393`). Nenhum lugar de `presentation/` chama `AnimatedDouble.keyframes` para desenhar | O motor entrega tudo: `valueAt` `keyframe.dart:498`. Cabe acrescentar a trilha ao `CurvaEmEdicao` (`editor_de_curva.dart:16`) e desenhar o percurso real em `_PintorDaCurva` (`editor_de_curva.dart:439`), com o trecho aberto em destaque. Nao precisa de tela nova: o painel ja tem 300 px e toma o cabecalho (`editor_screen.dart:299`) |
| **Velocity Graph** | **ESCONDIDO** | `Easing.speedAt` esta PRONTO: derivada analitica da bezier por bissecao, tratamento de ponta degenerada e de tangente vertical (`keyframe.dart:171-224`), com bateria de teste propria (`test/easing_speed_test.dart:10-62`) cujo cabecalho diz "O grafico de velocidade desenha Easing.speedAt" (`test/easing_speed_test.dart:5`). **Chamadores em `lib/`: ZERO** (grep `speedAt` = keyframe.dart + test) | Um segundo modo do mesmo pintor (`editor_de_curva.dart:439`), trocado por um botao no `_RailDaCurva` (`editor_de_curva.dart:137`), que hoje usa 2 das 4+ vagas da coluna. E o item de melhor razao valor/esforco desta frente inteira |
| **Bezier handles** | PASS | escolha da alca mais perto do dedo `editor_de_curva.dart:416-424`, movimento `:404-412`, desenho com raio 18 `:509-521` | Funciona bem (o `y` vai de -0,5 a 1,5 de proposito, `:410`). Herda o defeito de desfazer da linha "Bezier" |
| **Influence** | PASS (sem numero) | influence sao os proprios `x1`/`x2`, presos em 0..1 (`keyframe.dart:70`, `:104-107`) e movidos pelo dedo em `editor_de_curva.dart:406` | O dedo move o numero certo, mas o editor nao MOSTRA numero nenhum: nao ha campo, nem leitura, nem como digitar. `CampoDeValor` (`campo_de_valor.dart`) ja existe e caberia sob o quadro, ao lado de `_NomeDoPreset` (`editor_de_curva.dart:204`) |
| **Easing** | PASS | rail de presets com miniatura por curva `editor_de_curva.dart:264-311`; grava por trecho `setSegmentEase` `editor_controller.dart:4412`; "vale para todos" `applyEaseToAllSegments` `editor_controller.dart:4463` ligado em `controles_da_camada.dart:233` | Vale so para as 6 propriedades de transformacao. Para EFEITO a curva esta desligada a mao — ver a linha extra "Easing de efeito" abaixo |
| **Selecao multipla** | **FALTA** | `_QuadroState` guarda um indice so (`editor_de_curva.dart:396` `_preso`) e a curva editada e uma so (`editor_de_curva.dart:35`) | Cai junto com o Value Graph: sem grafico com varias marcas desenhadas nao ha o que selecionar |
| **Fit Graph** | **FALTA** | o quadro e sempre o retangulo inteiro do `LayoutBuilder`, sem escala nem deslocamento (`editor_de_curva.dart:389-393`, `:401-411`) | Nao ha o que enquadrar enquanto o grafico for uma caixa normalizada de um trecho. Cai junto com o Value Graph |
| **Fit Selection** | **FALTA** | idem, e nao ha selecao | idem |
| **Zoom** | **FALTA** (no grafico) | nenhuma escala no editor de curva. Existe zoom, mas e da LINHA DO TEMPO: `mapa_do_tempo.dart:97-106` (4..480 px/s), pinca na regua `linha_do_tempo.dart:496-505`, botao enquadrar `linha_do_tempo.dart:163-168` — outra coisa | Se o Value Graph nascer, o modelo de zoom ja esta escrito e testado em `MapaDoTempo`; da para reusar a conta tempo↔pixel |
| **Pan** | **FALTA** | `_QuadroState` consome `onPanStart/Update/End` inteiramente para as alcas (`editor_de_curva.dart:414-428`); nao sobra gesto nem existe transformacao de vista | idem |
| **Reset** | PASS (fraco) | tocar em "Linear" devolve a curva neutra: `presetsDeCurva[0]` `editor_de_curva.dart:39` → `setSegmentEase` | E reset DA CURVA, nao da vista (nao ha vista). O reset de verdade — apagar a animacao inteira de uma propriedade — existe no motor e nao tem porta: ver a linha extra "Limpar a animacao" |
| **Custom curve** | PASS (so bezier) | alca livre `editor_de_curva.dart:404-412`; o rotulo reconhece a curva a mao ("Bezier ajustada a mao") `editor_de_curva.dart:224` | Nao ha como SALVAR uma curva feita a mao, e as curvas parametrizadas do motor nao tem controle nenhum — ver a linha extra "Curvas parametrizadas" |
| **Atualizacao em tempo real** | PASS | `aplicar` grava a cada quadro do gesto (`editor_de_curva.dart:89-92`) e a previa continua a vista porque a curva e ferramenta e nao modal (`painel_da_camada.dart:244-257`, altura reservada em `editor_screen.dart:141-152`) | E o unico item da secao que esta redondo — e e tambem a causa do defeito de desfazer da linha "Bezier" |

## COMANDOS QUE EXISTEM E NAO TEM PORTA (busca inversa, encontrados durante a auditoria)

| item | veredicto | prova (arquivo:linha) | onde caberia |
|---|---|---|---|
| **Easing de efeito por trecho** | **ESCONDIDO** | `setEffectSegmentEase` `editor_controller.dart:4654` e `applyEaseToAllEffectSegments` `:4682` existem e SAO por segmento (recebem `segStartLocal`). A UI desliga o botao de curva do efeito (`controles_da_camada.dart:268`, `aoAbrirCurva: null`) com a justificativa escrita em `:265-267`: "O EFEITO NAO TEM CURVA POR TRECHO no motor de hoje". **Essa frase e falsa** — os dois comandos estao la, com 0 chamadores | Trocar `aoAbrirCurva: null` por um `CurvaEmEdicao` igual ao de `controles_da_camada.dart:228-235`, mirando `setEffectSegmentEase`. E copiar sete linhas |
| **Loop de keyframes (Ciclo, Pingue-pongue, Deslocado, Continuar)** | **ESCONDIDO** | `LoopSpec`/`LoopMode` completos e comentados como "o recurso mais usado do sistema de expressoes do AE" (`keyframe.dart:344-395`), remapeamento implementado em `:403-461`, comando `setPropertyLoop` `editor_controller.dart:1243`. Chamadores em `presentation/`: 0 | Duas marcas viram animacao infinita. Cabe como fileira de opcoes no proprio `EditorDeCurva` (o `_RailDaCurva`, `editor_de_curva.dart:137`) ou como `_Interruptor` (`controles_da_camada.dart:309`) na ferramenta da propriedade |
| **Inverter keyframes no tempo** | **ESCONDIDO** | `reversedInTime` `keyframe.dart:654` (e `:790` para Offset), comando `reversePropertyInTime` `editor_controller.dart:1275`. Chamadores: 0 | Acao na categoria "Camada" (`controles_da_camada.dart:90`, `_AcoesDaCamada`), que ja e a gaveta de acoes estruturais |
| **Keyframe de forma, mascara, animador de texto, grade e caminho** | **ESCONDIDO** | `toggleShapeParamKeyframe` `editor_controller.dart:5706`, `toggleMaskPathKeyframe` `:5079`, `toggleAnimatorPropKeyframe` `:5478`, `toggleGridParamKeyframe` `:4851`, `toggleShapeBezierKeyframe` `:6083`, `toggleMorphKeyframe` `:6139`, `toggleShapeItemTrackKeyframe` `:6238` — **todos com 0 chamadores**. Pior: o painel de Forma ja marca o parametro escolhido (`controles_da_camada.dart:436`) mas o rail dessa categoria devolve `const AlvoDoRail()` vazio (`controles_da_camada.dart:114`) — o realce promete um losango que nunca acende | `_alvoDaCategoria` (`controles_da_camada.dart:100-115`) ja e o ponto exato: acrescentar o caso `'forma'` chamando `toggleShapeParamKeyframe` com a chave de `parametroAbertoProvider`. Ha ainda travessia de estado: `parametroAbertoProvider` e o MESMO provider para Forma (`:437`) e para Efeitos (`:694`) |
| **Limpar a animacao de uma propriedade** | **ESCONDIDO** | `resetProp` `editor_controller.dart:4213` e `clearScaleKeyframes` `:2905`, ambos com 0 chamadores | Hoje so da para desanimar uma propriedade apagando marca por marca com toque longo. Cabe um terceiro botao no `RailEsquerdo` ou uma acao em `_AcoesDaCamada` (`controles_da_camada.dart:90`) |
| **Curvas parametrizadas (Degraus, Ciclico, Aleatorio, Deg. elastico, Mola)** | **ESCONDIDO** | cinco `EasingType` implementados, com `label` pronto para menu (`keyframe.dart:113-127`, `:255-264`) e parametros proprios (`count`, `smooth`, `intensity`, `response`, `damping`, `initialVelocity` — `keyframe.dart:74-86`). `presetsDeCurva` expoe 7 curvas fixas (`editor_de_curva.dart:38-46`) e `_Quadro` so mostra alca para `cubicBezier` (`editor_de_curva.dart:399`). Nenhum desses campos e escrito em `presentation/` (grep `count:/smooth:/response:` = 0 no editor de curva) | O rail de presets (`editor_de_curva.dart:264`) ja e uma `ListView` e aguenta mais miniaturas; os parametros cabem numa `LinhaDeParametro` (`linha_de_parametro.dart:25`) sob o quadro, aparecendo so quando o tipo escolhido tem parametro |
| **Cascata / stagger entre camadas** | **ESCONDIDO** | `cascadeSelection` `editor_controller.dart:6586`, `linkCascadeSelection` `:6610`, `cascadeLayerKeyframes` (em `domain/`), e `selectedLayerIdsProvider` `editor_controller.dart:63`. Chamadores em `presentation/`: 0 — a UI nova nem tem selecao de varias camadas | Depende primeiro de selecao multipla de camada na `VisaoGeralDasCamadas` (`visao_geral_das_camadas.dart:86`), que hoje seleciona uma so |
| **Expressao na propriedade** | **ESCONDIDO** | `AnimatedDouble.expression` `keyframe.dart:473`, compilacao com cache e teto (`:485-495`), erros de compilacao e de execucao expostos de proposito "para a interface poder mostrar" (`:479-483`), motor inteiro em `domain/expression_engine.dart`. Nenhuma tela de `presentation/` le ou escreve `expression` | Nao ha comando de controller para gravar expressao (so `withExpression` no dominio, `keyframe.dart:593`) — precisa de um `setPropertyExpression` antes de ter porta. A porta natural e o rail esquerdo (`rails_do_painel.dart:58`) |

---

## Frente 6

## VIDEO / AUDIO / COMPOSITING — auditoria contra a UI nova

Método: para cada item, achei o comando em `editor_controller.dart` e depois fiz a **busca inversa** — `grep` do nome do comando dentro de `lib/src/features/editor/presentation/` e, quando deu zero, dentro de `lib/` inteiro. "ZERO chamador" abaixo significa que rodei o grep no repositório todo, fora do próprio controller.

| item | veredicto | prova (arquivo:linha) | onde caberia / o que fazer |
|---|---|---|---|
| **VIDEO — Import** | QUEBRADO | motor `editor_controller.dart:398-416`; UI `adicionar_conteudo.dart:231`. A camada nasce com `Duration(seconds: 4)` (`:409`) e o `_probeDuration(...).then` (`:411-415`) chama `_replace` **depois**, num `_mutate` separado (`:90-124`). Como `addVideoLayer` é estrutural, `_ultimaFoiEstrutural` fica `true` e a correção da duração **empilha um segundo undo** | O `.then` tem de rodar dentro do mesmo passo (ou fora da pilha de undo). Hoje: importar um vídeo de 3 min → primeiro "desfazer" devolve o clipe a 4 s em vez de remover a camada; e se a pessoa aparar nesses milissegundos, o probe sobrescreve a aparagem |
| **VIDEO — Trim** | QUEBRADO | motor `editor_controller.dart:3793-3868`; UI `visao_geral_das_camadas.dart:277` (`trimLayerStart`) e `:279` (`trimLayerEnd`). `trimLayerEnd` (`:3847-3868`) só tem piso de 100 ms — **nenhum teto contra o fim da mídia**, e `VideoLayer` não tem campo `sourceDuration` (`layer.dart:457-511`, só `sourceOffset`) | Encurtar está certo (o `sourceOffset` é ajustado corretamente em `:3813-3822`). Esticar não: a alça direita passa do fim do arquivo e o clipe vira mais longo que a mídia. Precisa guardar a duração da fonte na `VideoLayer` e travar em `trimLayerEnd`/`trimLayerStart` |
| **VIDEO — Split** | PASS | motor `editor_controller.dart:3872-3941` (fatia `sourceOffset`, reversa e time-remap, e reaponta `transitionIn`); UI `controles_da_camada.dart:1082`, no cartão "Camada" (`painel_da_camada.dart:151`) | — |
| **VIDEO — Crop** | FALTA | grep `crop|recorte de quadro` em `domain/` e no controller: zero. O único recorte geométrico é máscara (`mask.dart:244` `BezierPath.rect`), e máscara não tem UI | Não existe comando de crop. O caminho barato é `addMask` com `BezierPath.rect` mais quatro alças, como categoria nova em `categoriasDaCamada` (`painel_da_camada.dart:112`) + ramo no `switch` de `controles_da_camada.dart:83` |
| **VIDEO — Scale** | PASS | motor `editor_controller.dart:4066/4078/4088`; UI `painel_de_transformacao.dart:284-288` (modo `escalar`, `:15`) | — |
| **VIDEO — Position** | PASS | motor `editor_controller.dart:4000`; UI `painel_de_transformacao.dart:124,131` (modo `mover`) | — |
| **VIDEO — Rotation** | PASS | motor `editor_controller.dart:4138`; UI `painel_de_transformacao.dart:229` (`dial_de_angulo`) | — |
| **VIDEO — Speed** | ESCONDIDO | motor `editor_controller.dart:2923-2967` (`setClipSpeed`, com ripple do que vem depois), `:2998` `applySpeedRamp`, `:3016` `setClipTimeRemapEnabled`, `:3070` `setClipSpeedBlur`, `:3010` `setClipInterpolacao`. **ZERO chamador em `lib/`** | Categoria "Velocidade" em `categoriasDaCamada` (`painel_da_camada.dart:112`) só para `VideoLayer`/`AudioLayer`, corpo novo no `switch` de `controles_da_camada.dart:83`, usando `LinhaDeParametro` como o `_Volume` (`:462`) |
| **VIDEO — Reverse** | ESCONDIDO | motor `editor_controller.dart:3085-3096` (+ `reverseNeedsProxy` `:3077`). **ZERO chamador** | Ação em `_AcoesDaCamada` (`controles_da_camada.dart:1059-1096`), ao lado de "Dividir no cabeçote". Ela já sabe desabilitar com motivo (`porQueNao`), que é exatamente o que `setClipReverse` devolvendo `false` pede |
| **VIDEO — Freeze** | ESCONDIDO | motor `editor_controller.dart:3394-3460` (`freezeFrame`, com `FreezePlacement.insideClip`/`separateClip`). **ZERO chamador** | Mesma lista `_AcoesDaCamada` (`controles_da_camada.dart:1075`), com a mesma regra de "cabeçote dentro da camada" que o `podeDividir` já calcula em `:1071` |
| **VIDEO — Opacity** | PASS | motor `editor_controller.dart:4147`; UI `controles_da_camada.dart:293,295` + rail de keyframe `:225` | — |
| **VIDEO — Blend** | ESCONDIDO | motor `editor_controller.dart:4205` (`setBlendMode`) e `:4193` (`setCustomBlend`); o compositor **já honra** os dois: `palco_de_previa.dart:963,1382-1383` e `:625`. **ZERO chamador** dos dois setters | Categoria "Mistura" em `painel_da_camada.dart:112` (grade de modos, como o `_CatalogoDeEfeitos` em `controles_da_camada.dart:898`). Hoje `grep -i "mistura|blend"` nos widgets de controle dá zero |
| **VIDEO — Frame Rate** | ESCONDIDO | motor `editor_controller.dart:248` (`setComposition({aspectRatio, resolutionHeight, fps})`) — **ZERO chamador em `lib/` inteiro**. O fps só é escolhido na criação (`new_project_sheet.dart:85-86`), e `ExportSettings.fps` (`export_settings.dart:63,73`) nunca recebe valor (`grep "copyWith(fps"` = zero) | Um item no cabeçalho (`editor_screen.dart:_Cabecalho`, ao lado do `AlternadorDeVista` em `:341`), ou categoria "Composição" que não dependa de camada. Hoje projeto criado a 30 fps morre a 30 fps |
| **VIDEO — extração de áudio** | PASS | UI `adicionar_conteudo.dart:250` → `importAudioFile(em, fromVideo: true)` (`editor_controller.dart:458-468`) → `media_import_service.dart:110-148`, que roda ffmpeg de verdade (`-map 0:a:0 -vn -c:a aac`) | Só existe **na importação**. Separar o áudio de uma `VideoLayer` que já está na linha não existe (grep `detach|separar audio` = zero). Caberia em `_AcoesDaCamada` |
| **AUDIO — Import** | PASS | motor `editor_controller.dart:458-468`; UI `adicionar_conteudo.dart:244` ("Arquivo") | — |
| **AUDIO — Waveform** | ESCONDIDO | motor completo: `streaming_waveform.dart` (filtros + LUFS), `media_preview_service.dart:115` `ensureWaveform`, `:74` `peaksOf`, `:81` `pyramidOf`, `:84` `stripOf`. **`grep MediaPreviewService lib/src/features/editor/presentation/` = zero**. O pintor da faixa desenha só barra colorida + nome + losangos (`linha_do_tempo.dart:1082-1160`) | `_PintorDaFaixa` (`linha_do_tempo.dart:1068`) é o lugar exato: já recebe `mapa` e `camada`, e já clipa no `RRect` (`:1119`). Falta pedir `peaksOf`/`stripOf` e desenhar dentro do clip. Sem onda, cortar música na batida é adivinhação |
| **AUDIO — Volume** | ESCONDIDO | `AudioLayer.volume` existe (`layer.dart:1431,1465`) e o motor o usa (`audio_mix.dart:159-163` `layerAudioGainAt`, e o preview aplica em `video_layer_manager.dart:395-418`). Mas o cartão "Volume" só nasce para `VideoLayer` (`painel_da_camada.dart:135-140`) porque `editVideoVolume` **recusa** o que não for vídeo (`editor_controller.dart:6805-6809`) — a própria ficha do painel admite isso em `painel_da_camada.dart:103` | Ou generalizar `editVideoVolume` para `AudioLayer`, ou expor `updateAudioSpec(..., gain:)` (`editor_controller.dart:2240`). O cartão já existe (`controles_da_camada.dart:462-484`); basta soltar o `as VideoLayer` de `:470` e a guarda de tipo de `painel_da_camada.dart:135`. Importar uma música e não poder baixar o volume dela é o buraco mais visível desta frente |
| **AUDIO — Mute** | QUEBRADO | `AudioSpec.muted` existe (`layer.dart:343,362`) e o mix zera na hora (`audio_mix.dart:157`), mas **ZERO chamador** de `updateAudioSpec` na UI. O único controle parecido é o olho (`linha_do_tempo.dart:296`, `visao_geral_das_camadas.dart:380` → `toggleHidden`), e ele **só afeta o desenho**: o preview filtra por `rendersInPreview` (`palco_de_previa.dart:370`, `video_project.dart:244`), mas o `VideoLayerManager` recebe `project.layers` cru (`editor_screen.dart:59-64`) e monta a lista de mídia sem olhar `meta.hidden` (`video_layer_manager.dart:290-320`) | Olho fechado numa trilha de áudio: **continua tocando no preview** e **some na exportação** (`editor_controller.dart:611-620` remove escondidas). Preview e arquivo discordam. Botão de mudo próprio na `PilulaDaCamada` (`linha_do_tempo.dart:1011-1063`, que hoje só tem o olho) e `hidden` respeitado no `sync` |
| **AUDIO — Fade In** | ESCONDIDO | `AudioSpec.fadeIn` (`layer.dart:340,356`), aplicado em `audio_mix.dart:168-173` e no preview (`video_layer_manager.dart:395`). Setter: `updateAudioSpec` (`editor_controller.dart:2240`) — chamado só por `autoedit_runner.dart:206`, e o `AutoEditScreen` **não tem rota** (`autoedit_screen.dart:22`; `grep AutoEditScreen` fora do arquivo = zero) | Categoria "Som" em `painel_da_camada.dart:112` para `AudioLayer`/`VideoLayer`, com duas `LinhaDeParametro` em `controles_da_camada.dart:83` |
| **AUDIO — Fade Out** | ESCONDIDO | idem: `layer.dart:341,357`; `audio_mix.dart:171` | idem acima — a mesma categoria "Som" |
| **AUDIO — Split** | PASS | motor `editor_controller.dart:3913-3921` (ramo `AudioLayer`, ajusta `sourceOffset` pela velocidade); UI `controles_da_camada.dart:1082` | — |
| **AUDIO — Trim** | QUEBRADO | motor `editor_controller.dart:3827-3841` (ramo `AudioLayer` de `trimLayerStart`, correto); UI `visao_geral_das_camadas.dart:277,279`. Mesmo defeito do vídeo: `trimLayerEnd` (`:3847`) não tem teto contra o fim do arquivo | Travar em `trimLayerEnd` com a duração real da fonte (que `importAudioFile` já conhece — `editor_controller.dart:465` — e joga fora) |
| **AUDIO — Speed** | ESCONDIDO | motor `editor_controller.dart:2923` (ramo `AudioLayer l => l.copyLayer(speed: v, ...)` em `:2946`) + `setClipPreservePitch` `:2986` (o motor sabe manter ou não o tom). **ZERO chamador** | Mesma categoria "Velocidade" proposta para vídeo, com um `_Interruptor` (`controles_da_camada.dart:1176`) para "manter o tom" |
| **AUDIO — Sync** | FALTA | Não existe alinhamento por forma de onda (grep `sincron|align.*audio` no controller: só comentários — `:3701`, `:4100`). Também **não há imantação** na linha do tempo (`grep snap|imanta` em `presentation/` só acha `SnapshotWidget`) nem passo por quadro: `moveLayer` recebe tempo contínuo do dedo (`visao_geral_das_camadas.dart:275`) | O que existe e serviria de base é a grade de batidas (`editor_controller.dart:2373` `detectBeatsInto`, `:2454` `distributeAtMarkers`). Sincronizar hoje é arrastar no olho, sem onda e sem ímã: erro de dezenas de milissegundos garantido |
| **AUDIO — múltiplas trilhas** | PASS | motor `video_layer_manager.dart:290-320` monta um tocador por `AudioLayer` (e um extra por `VideoLayer` com áudio processado), com ganho por camada em `:395-418` e ducking pré-calculado em `:325-330`; UI: a pilha aceita N camadas (`visao_geral_das_camadas.dart:242`) e o `+` cria quantas se quiser (`adicionar_conteudo.dart:244`) | Toca; o que não dá é **mexer** em nenhuma delas (ver Volume/Mute/Fade acima) |
| **AUDIO — Markers** | ESCONDIDO | motor `editor_controller.dart:3527` `toggleMarker`, `:3552` `renameMarker`, `:3567` `moveMarker`, `:3584` `setMarkerColor`, `:3598` `removeMarker`, `:3612` `clearMarkers`, `:3619/3626` navegação, `:3980` `addMarkers`, `:2428` `cutAtMarkers`. **ZERO chamador de todos**. As "marcas" que aparecem na régua são traços de tempo (`linha_do_tempo.dart:559-560`), outra coisa | Botão no `_Transporte` (`linha_do_tempo.dart:337-402`, hoje 7 alvos: desfazer, refazer, início, tocar, fim, duplicar, enquadrar) e desenho em `_PintorDaRegua` (`linha_do_tempo.dart:541`), que já sabe converter tempo→x |
| **AUDIO — detecção de batida** | ESCONDIDO | motor `editor_controller.dart:2373-2403` (`detectBeatsInto`, com banda, sensibilidade e estimativa de BPM), `:2409` `setBpm`, `:2421` `clearBeats`, `:2490` `beatsOf`, `:2856` `applyBeatPulse`, `:2428/2454` cortar/distribuir na batida. **ZERO chamador de todos** | Ação em `_AcoesDaCamada` para camada de áudio ("achar as batidas"), grade desenhada em `_PintorDaRegua` (`linha_do_tempo.dart:541`) e "distribuir na batida" como ação de projeto. Está tudo pronto e nada chega ao dedo |
| **COMP — ordem de camada** | PASS | motor `editor_controller.dart:3727-3736`; UI `visao_geral_das_camadas.dart:228` (alça de três riscos, `_AlcaDeOrdem` em `:418`), com uma mutação só no soltar (`:210-228`). Sinal confere: dedo para baixo → índice maior → mais fundo | `reorderLayers` (lote, `:3743`) é código morto — não há seleção múltipla (ver Group) |
| **COMP — blend modes** | ESCONDIDO | igual a "VIDEO — Blend": `editor_controller.dart:4193,4205`; renderizador pronto em `palco_de_previa.dart:963,1382`; `AureaBlend` com compositor de dois andares (`layer.dart:94-100`, `custom_blend.dart`). **ZERO chamador** | Categoria "Mistura" em `painel_da_camada.dart:112` + grade em `controles_da_camada.dart` no molde do `_CatalogoDeEfeitos` (`:898`) |
| **COMP — Opacity** | PASS | `editor_controller.dart:4147`; UI `controles_da_camada.dart:293` | — |
| **COMP — Alpha** | PASS | O alfa da mídia é preservado no compositor (`palco_de_previa.dart:934-952`, `dstIn`), e os comandos que mexem em alfa chegam pela UI: `addEffect` (`controles_da_camada.dart:946`) dá acesso ao catálogo inteiro (`effect.dart:9-71`), com `chromaKey`, `lumaKey`, `colorKey` e `unmult` (`effect.dart:25,57-60`) | O único ramo de alfa que **não** chega é o alpha matte — ver "Track matte" |
| **COMP — Masks** | ESCONDIDO | motor inteiro e sem porta: `editor_controller.dart:4903` `addMask`, `:4933` `removeMask`, `:4941` `reorderMask`, `:4954` `updateMask`, `:4968` `cycleMaskMode`, `:4975` `toggleMaskInverted`, `:4980` `editMaskParam`, `:5005` `toggleMaskFeatherAxes`, `:5014` `editMaskPath`, `:5032` `replaceMaskPath`, `:5043` `setMaskFromOwnShape`, `:5079` `toggleMaskPathKeyframe`, `:4911` `applyMaskReveal` (7 presets em `mask.dart:499`). 7 modos (`mask.dart:495`), caminho animável (`mask.dart:422`), e o compositor **já desenha** (`palco_de_previa.dart:449-470`). **ZERO chamador dos treze** | Categoria "Máscara" em `painel_da_camada.dart:112`; o mais barato para destravar já é `applyMaskReveal` (7 presets = 7 botões em `controles_da_camada.dart`), porque não precisa de edição de caminho no palco — que também não existe: `palco_de_previa.dart` **não tem um único `GestureDetector`** (grep = zero) |
| **COMP — Clipping** | ESCONDIDO | `GroupLayer.clipToComp`, escrito só por `updatePrecomp` (`editor_controller.dart:5260,5271`). **ZERO chamador** | Um `_Interruptor` (`controles_da_camada.dart:1176`) numa categoria "Grupo", visível quando `camada is GroupLayer` em `painel_da_camada.dart:112` |
| **COMP — Track matte** | ESCONDIDO | motor `editor_controller.dart:5112` `setMatteFromAbove`, `:5126` `setMatte`, `:5100` `matteSourceAbove`; 5 modos (`mask.dart:668` alpha/alphaInvert/luma/lumaInvert) e o compositor **já monta o grupo isolado e converte o canal** (`palco_de_previa.dart:586-620`, `:883-905`), inclusive escondendo a camada-fonte (`:361-364`). **ZERO chamador** | `setMatteFromAbove` foi feito para um toque só ("usar a camada de cima como recorte") e cabe direto em `_AcoesDaCamada` (`controles_da_camada.dart:1059`). É o item com maior razão função-pronta/esforço-de-UI desta frente |
| **COMP — Parent/Child** | ESCONDIDO | `linkProperty(target, LayerProp.parent, source, t)` (`editor_controller.dart:6724`, ramo `parent` em `:6745-6770`) captura o transform efetivo do pai e evita salto; `unlinkProperty` `:6794`; cascata `:6586,6610`. **ZERO chamador de todos** | Linha "Pai" na categoria "Mover e transformar" (`painel_de_transformacao.dart`, ao lado dos campos em `:112`), com uma lista das outras camadas. Sem isto o objeto nulo não serve para nada (linha abaixo) |
| **COMP — Null** | QUEBRADO | UI existe: `adicionar_conteudo.dart:131` → `addNullLayer` (`editor_controller.dart:1319`). Mas a razão de ser do nulo — "vire pai de outras camadas" (`layer.dart:1572-1573`) — depende de `linkProperty`, que tem **ZERO chamador**. O outro uso, a grade (`setGridAssets`, `editor_controller.dart:4742`), também tem zero. E `palco_de_previa.dart` só cita `NullLayer` dentro do rig de grade (`:423-425`): ele nem é desenhado como wireframe | Tocar em "Nulo" hoje cria uma camada **invisível, sem filhos possíveis e sem desenho** — um item de menu que não faz nada. Ou some do `catalogosDeAdicao` (`adicionar_conteudo.dart:126-133`) até o parenting existir, ou o parenting entra junto |
| **COMP — Group** | ESCONDIDO | `groupLayers(List<String>)` (`editor_controller.dart:6445`), `groupLayer` `:6487`, `ungroupLayer` `:6509`, `enterGroup` `:520`, `exitGroup` `:543`. **ZERO chamador de todos**. Pior: `multiSelectProvider` (`editor_controller.dart:64`) só é **limpo** (`:242,537,554,2229`) e nunca preenchido — não há como escolher duas camadas | Seleção múltipla por toque longo na pilha (`visao_geral_das_camadas.dart:352-359`, onde o `onTap` já decide entre selecionar e abrir ferramentas) + ação de agrupar em `_AcoesDaCamada`. E `enterGroup`/`exitGroup` precisam de porta: hoje um `GroupLayer` (que `addSvgLayers` cria sozinho, `editor_controller.dart:348`) é uma caixa preta sem entrada |
| **COMP — Pre-compose** | ESCONDIDO | precomp É `GroupLayer`: `updatePrecomp` (`editor_controller.dart:5260`), `enablePrecompTimeRemap` `:5307`, `setPrecompContentTime` `:5321`, `freezePrecompAt` `:5330`, `reversePrecomp` `:5341`, mais `collapse` e `sourceDuration` (`layer.dart:1125,1157`). **ZERO chamador dos cinco** | Depende de Group acima. Depois disso, categoria "Precomp" em `painel_da_camada.dart:112` quando `camada is GroupLayer`, com as quatro ações em `controles_da_camada.dart:83` |

**Placar desta frente: 38 itens — 12 PASS, 5 QUEBRADO, 21 ESCONDIDO/FALTA.** O padrão é um só: o motor tem tudo, a UI nova ligou transformação, opacidade, efeitos e três ações de camada, e parou. Não é lista de desejos — é `grep` de comando por comando dentro de `presentation/`.

### Os três achados mais graves

**1. Áudio é praticamente somente-leitura, e o único botão que parece mudo mente.** Dá para importar quantas trilhas se quiser (`adicionar_conteudo.dart:244`) e o mixer do motor faz volume, ganho, mudo, fade e ducking (`audio_mix.dart:150-176`, aplicado no preview em `video_layer_manager.dart:395-418`) — mas nenhum desses chega ao dedo: o cartão "Volume" existe só para `VideoLayer` porque `editVideoVolume` recusa áudio (`editor_controller.dart:6805-6809`, admitido em `painel_da_camada.dart:103`), e `updateAudioSpec` (`editor_controller.dart:2240`) tem zero chamador na UI (o único caller, `autoedit_runner.dart:206`, mora numa tela sem rota). O olho, que a pessoa vai usar como mudo, **não silencia no preview** (`video_layer_manager.dart:290-320` nunca olha `meta.hidden`) e **silencia na exportação** (`editor_controller.dart:611-620`) — preview e arquivo final discordam. Somando a falta de forma de onda (`MediaPreviewService` inteiro sem um chamador em `presentation/`) e de imantação, editar música neste app é impossível hoje.

**2. Compositing existe inteiro no renderizador e não tem um único caminho na UI.** Máscara (13 comandos, `editor_controller.dart:4903-5096`, desenhada em `palco_de_previa.dart:449-470`), track matte (`:5100-5137`, com o grupo isolado e a conversão de canal já prontos em `palco_de_previa.dart:586-620`), blend mode (`:4193,4205`, honrado em `palco_de_previa.dart:963,1382`), parenting (`:6724`), grupo e precomp (`:6445,5260`): **zero chamadores, todos**. E a raiz que trava metade disso é estrutural: `multiSelectProvider` (`editor_controller.dart:64`) só é limpo, nunca preenchido, e `palco_de_previa.dart` não tem nenhum `GestureDetector` — sem seleção múltipla não há grupo, e sem gesto no palco não há como desenhar máscara. `setMatteFromAbove` (`:5112`) é a maior função pronta por menos UI de toda esta frente: um item em `_AcoesDaCamada` e o recorte por camada volta a existir.

**3. Dois defeitos de precisão nas únicas operações que a UI de fato liga.** (a) Importar vídeo custa **dois passos de desfazer**: a camada entra com 4 s de mentira e o `_probeDuration(...).then` corrige num `_mutate` separado (`editor_controller.dart:409-415` com `:113-119`), então o primeiro "desfazer" devolve o clipe ao provisório em vez de removê-lo — e a mesma correção assíncrona sobrescreve qualquer aparagem feita nesse meio-tempo. (b) A alça de aparar não tem teto: `trimLayerEnd` (`editor_controller.dart:3847`) só respeita o piso de 100 ms, e `VideoLayer` sequer guarda a duração da fonte (`layer.dart:457-511`) — a duração real chega no probe e é jogada fora. Arrastar a ponta direita estica o clipe além do fim da mídia, sem aviso. Junte a isso o "Nulo" do menu de adição (`adicionar_conteudo.dart:131`), que cria uma camada invisível, sem filhos possíveis e nem desenhada, e o resumo é: das poucas portas abertas, três estão tortas.

---

## Frente 7

## Auditoria — PREVIEW · EXPORT · ASSETS · UNDO/COPY

Regra usada: PASS exige as DUAS pontas citadas (comando no motor + widget que chama). Busca inversa feita por `grep` do nome do comando dentro de `presentation/`; comando com 0 chamadores nao e PASS, seja qual for o nome.

| item | veredicto | prova (arquivo:linha) | onde caberia / o que fazer |
|---|---|---|---|
| **PREVIEW — Play** | PASS | motor `playback_controller.dart:104 play()` / `:124 toggle()`; UI `linha_do_tempo.dart:363-369` (alvo 4 do transporte) | — |
| **PREVIEW — Pause** | PASS | motor `playback_controller.dart:115 pause()`; UI `linha_do_tempo.dart:366-369` (mesmo alvo, icone troca) | — |
| **PREVIEW — Stop** | PASS (com ressalva de nome) | UI `linha_do_tempo.dart:355-361` faz `playback.pause(); playback.seek(Duration.zero)` — e stop, so nao se chama assim; motor `playback_controller.dart:115,:140` | Nada a fazer alem de aceitar que "Inicio" e o Stop. |
| **PREVIEW — Frame Forward** | FALTA | nao existe passo de quadro em lugar nenhum: `PlaybackController` so tem `play/pause/toggle/seek` (`playback_controller.dart:104-152`); grep por `stepFrame\|proximoQuadro\|frameForward\|avancarQuadro` em `lib/` = 0 | `_Transporte` esta fechado em 7 alvos por decisao de projeto (`linha_do_tempo.dart:312-322`). Cabe como duas setas coladas na CAPSULA DO TEMPO, que ja e desenhada em `_PintorDaRegua._pintarCapsula` (`linha_do_tempo.dart:668`), ou como toque longo em Inicio/Fim. |
| **PREVIEW — Frame Back** | FALTA | idem acima | idem acima |
| **PREVIEW — Loop** | ESCONDIDO | motor `playback_controller.dart:34 loop` e `:48-55` (o laco ja funciona); unico lugar do app que liga: `settings/presentation/estresse3d_screen.dart:399`. Zero uso no editor | `_Transporte` (`linha_do_tempo.dart:323`) — mas ele nao tem alvo livre; o lugar honesto e a REGUA, como um pequeno interruptor na ponta da barra de rolagem (`linha_do_tempo.dart:629`), que ja e a peca que fala de "extensao do projeto". |
| **PREVIEW — Current Time** | **QUEBRADO** | a capsula le o fps do PROJETO (`linha_do_tempo.dart:671` → `relogioDeQuadros`, `:719`), mas o relogio quantiza numa grade FIXA de 30: `PlaybackController.compositionFps = 30` (`playback_controller.dart:41`) e usado em `_naGrade` (`:132`) e NUNCA e atribuido — grep em `lib/` e `test/` devolve so a declaracao e o uso | Uma linha em `editor_screen.dart:41-45` (onde o `PlaybackController` nasce) + refresco no `build` quando `project.fps` muda. |
| **PREVIEW — Preview Quality** | ESCONDIDO | o rascunho automatico existe e funciona (`palco_de_previa.dart:284 _rascunho`, `:4055`); o unico controle HUMANO e o teto 3D e mora em Ajustes: `settings/presentation/settings_tab.dart:501 _SegmentedRow<TetoDeQualidade3D>` → `qualidade3d_controller.dart:103 definirTeto`. Nenhum controle na tela de edicao, e nenhum que valha para a composicao 2D | `PainelDaCamada` nao serve (e por camada). Cabe no `_Cabecalho` do editor (`editor_screen.dart:268-368`), ao lado do `AlternadorDeVista`, como um chip Rascunho/Cheia. |
| **PREVIEW — Resolution** | ESCONDIDO | o preview nao tem resolucao propria (desenha no tamanho do projeto e o `FittedBox` escala — `editor_screen.dart:211-232`), e isso esta certo. O que nao tem porta e a resolucao DA COMPOSICAO: `editor_controller.dart:248 setComposition` tem **0 chamadores** fora do controller; so se escolhe ao criar (`projects/presentation/new_project_sheet.dart:85-87`) | Uma categoria "Composicao" no `PainelDaCamada` nao cabe (e por camada). Cabe no `_Cabecalho` (`editor_screen.dart:268`), no toque sobre o NOME DO PROJETO (`:319-330`), que hoje nao faz nada. |
| **PREVIEW — Fullscreen** | FALTA | `application/ui/editor_session.dart:75 previewExpanded` e `:188 togglePreviewExpanded` existem, mas o ARQUIVO INTEIRO nao tem um unico uso fora dele (grep `EditorSession` em `lib/` = 0). E bandeira morta: ninguem desenha nada a partir dela | `_Cabecalho` (`editor_screen.dart:268`) e a conta de altura em `editor_screen.dart:108-170`, que ja e o unico lugar que decide o tamanho da previa. |
| **PREVIEW — progresso de cache/render** | FALTA (no editor) | `ProxyService.progress` existe (`proxy_service.dart:42`) mas **ninguem gera proxy**: `ensureProxy` (`:82`) tem 0 chamadores fora do proprio arquivo — logo `playbackPath` (`:59`) devolve sempre o original. `PreviewStats` e escrito (`palco_de_previa.dart:337,:345`) e so LIDO na tela de estresse (`settings/presentation/estresse3d_screen.dart:432`) | A barra de rolagem do topo da regua (`linha_do_tempo.dart:629-640`) e o lugar canonico da barra de cache — ela ja e a unica coisa colorida da regua e ja fala de proporcao do projeto. |
| **PREVIEW — Scrubbing** | PASS (com duas ressalvas) | motor `playback_controller.dart:140 seek`; UI regua `linha_do_tempo.dart:498-518` → `_levar` (`:538`), e faixa da camada `linha_do_tempo.dart:903-906`. Ressalvas: gruda na grade errada (ver Current Time) e o proxy que existe justamente para o scrub ficar continuo nunca e construido | — |
| **EXPORT — Export** | PASS | UI `editor_screen.dart:178-186` (botao lima) → `ExportVideoScreen`; motor `export/application/export_engine.dart:38` e `export_video_screen.dart:106 _rodar()` | — |
| **EXPORT — Resolution** | ESCONDIDO | `export/domain/export_settings.dart:34 ExportSize` + `:112 resolve()` existem e sao testados (`test/export_settings_test.dart:14-38`), mas o UNICO construtor real e `const ExportVideoScreen()` **sem settings** (`editor_screen.dart:182`) → sempre `ExportSize.original` | O `ExportVideoScreen` ja tem a superficie: `_rodape()` (`export_video_screen.dart:620`). Falta uma FASE `_Fase.ajustes` antes do `_rodar()` (`:81`), com os mesmos `_SegmentedRow` que o `new_project_sheet` usa. |
| **EXPORT — FPS** | ESCONDIDO | `export_settings.dart:73 fps` + `:119 resolveFps` (testado em `test/export_settings_test.dart:51-59`); inalcancavel pelo mesmo motivo (`editor_screen.dart:182`) | idem: fase de ajustes no `ExportVideoScreen`. |
| **EXPORT — Format (MP4 / sequencia PNG)** | ESCONDIDO | `export_settings.dart:21 pngSequence`; o caminho inteiro existe e esta escrito (`export_video_screen.dart:199`, `:287-297` → `export_engine.dart:795 saveSequence`) e nunca e alcancado | idem: fase de ajustes no `ExportVideoScreen`. |
| **EXPORT — Codec (H.264 / HEVC)** | ESCONDIDO | `export_settings.dart:30 hevc`, consumido em `export_video_screen.dart:222`; sempre `h264` porque as settings nunca sao passadas (`editor_screen.dart:182`) | idem. |
| **EXPORT — Quality / Bitrate** | **QUEBRADO** (alem de escondido) | DUAS fontes de verdade para o mesmo botao: `ExportVideoScreen.quality` (String, `export_video_screen.dart:37`) e `ExportSettings.quality` (`export_settings.dart:64,:76`). `export_engine.dart:865 taxaDeBits` so olha as settings quando ha `bitrateMbps`, HEVC ou tamanho != original; fora disso usa `PlatformEncoder.bitrateFor(..., widget.quality)` (`export_video_screen.dart:221`). Ou seja `ExportSettings(quality:'alta')` sozinho e **silenciosamente ignorado** | Matar o parametro `quality` da tela (`export_video_screen.dart:37`) e deixar `settings.quality` mandar sempre; `taxaDeBits` passa a chamar so `settings.bitrateFor`. |
| **EXPORT — Audio** | FALTA | o audio e sempre juntado, AAC 192k fixo (`export_engine.dart:911-916`); `ExportSettings` (`export_settings.dart:60-68`) nao tem NENHUM campo de audio — nem liga/desliga, nem taxa | Campo novo em `ExportSettings` + linha na fase de ajustes do `ExportVideoScreen`. Um "exportar so a imagem" e barato: `juntarAudio` ja sai pelo atalho quando nao ha trilha (`export_engine.dart:893`). |
| **EXPORT — Destination** | FALTA | destino unico e fixo: `Documents/exports` (`export_engine.dart:775-788`), mais a galeria se o interruptor GLOBAL estiver ligado (`export_video_screen.dart:529-531` lendo `settings_tab.dart:142`). Nao ha escolher pasta nem compartilhar; o que a tela oferece e "Copiar caminho" (`export_video_screen.dart:738`) | `_rodape()` do estado pronto (`export_video_screen.dart:658`): trocar "Copiar caminho" por um Compartilhar de sistema resolve o caso real do celular. |
| **EXPORT — fundo transparente** | **QUEBRADO** | `export_settings.dart:87 keepsAlpha` promete alfa na sequencia PNG, mas o que e capturado esta dentro de `ColoredBox(color: project.backgroundColor)` (`export_video_screen.dart:573`), incondicional — nao pergunta por `keepsAlpha`. E o fundo do projeto nasce OPACO `0xFF000000` (`domain/video_project.dart:114`) e `editor_controller.dart:259 setBackgroundColor` tem **0 chamadores**. Todo quadro sai opaco, sempre | Duas correcoes, ambas pequenas: `ColoredBox` so quando `!settings.keepsAlpha` (`export_video_screen.dart:573`), e uma porta para `setBackgroundColor` (cabe no toque do nome do projeto no `_Cabecalho`, `editor_screen.dart:319`). |
| **EXPORT — Progress** | PASS | motor `export_video_screen.dart:100 _passo` alimentado por `export_engine.dart` (`encode(onProgress:)`, `:944`); UI `export_video_screen.dart:802 LinearProgressIndicator` + porcentagem `:790` | — |
| **EXPORT — Cancel** | PASS fraco | `export_video_screen.dart:605-614`: o `x` da barra chama `_engine?.cancel()` (`export_engine.dart:56`) e sai. Sem rotulo, sem `Semantics`, sem confirmacao — e o mesmo glifo que em toda outra tela quer dizer "fechar", nao "abandonar dez minutos de render" | Trocar o `x` por um botao "Cancelar" no `_rodape()` enquanto `_fase` esta em andamento (`export_video_screen.dart:772`), onde o olho ja esta. |
| **EXPORT — Retry** | FALTA | o rodape de erro (`export_video_screen.dart:630-668`) mostra o texto e mais nada: nenhum botao, e `_rodar()` so e chamado uma vez em `initState` (`:81`). Sair e entrar de novo e o unico caminho | O proprio `_rodape()` do erro (`export_video_screen.dart:630`): um botao "Tentar de novo" chamando `_rodar()` depois de zerar `_erro`/`_fase`. |
| **EXPORT — Error state** | PASS | motor `export_video_screen.dart:317-339` + `_explicar` (`:341-358`, traduz ENOSPC/OOM/codec em frase util) e `export_engine.dart:847 conferirEspaco` (falha ANTES de comecar); UI `export_video_screen.dart:630-668` | — |
| **ASSETS — Media (biblioteca)** | FALTA | nao ha navegador de midia: so os seletores do sistema. O `media/application/gallery_service.dart:27 GalleryService` (albuns, paginacao, permissao) esta inteiro e tem **zero** consumidores — `galleryServiceProvider` (`:96`) so aparece na propria definicao | Uma familia nova em `categoriasDeAdicao` (`adicionar_conteudo.dart:115`) abrindo o `PainelCentralDeAdicao` (`:355`) com a grade de assets em vez de cartoes de comando. O painel do meio ja e um `GridView` (`:437`). |
| **ASSETS — Images** | PASS | UI `adicionar_conteudo.dart:218`; motor `editor_controller.dart:2166 importImageFromGallery` → `:369 addImageLayer` | — |
| **ASSETS — Video** | PASS (mas ver linha seguinte) | UI `adicionar_conteudo.dart:231`; motor `editor_controller.dart:398 importVideoFromGallery` → `:381 addVideoLayer` | — |
| **ASSETS — Video: a previa depois de importar** | **QUEBRADO** | `_syncVideos` so esta pendurado em `_playback.time` e `_playback.playing` (`editor_screen.dart:47-48`) e roda uma vez pos-quadro (`:52`). Importar nao mexe no relogio: `abrirAdicaoDeConteudo` chama `playback.pause()` num relogio JA parado (`painel_da_camada.dart:319`) e `ValueNotifier` nao notifica valor igual (`playback_controller.dart:115-121`). Logo `VideoLayerManager.sync` (`video_layer_manager.dart:258`) nunca roda, `controllerFor` devolve null e a previa mostra o icone de filme (`palco_de_previa.dart:4144-4155`) ate alguem arrastar o cabecote | Escutar o projeto: um `ref.listen(editorControllerProvider, ...)` no `EditorScreen` chamando `_syncVideos` (`editor_screen.dart:56`). |
| **ASSETS — Audio** | PASS | UI `adicionar_conteudo.dart:244` e `:250`; motor `editor_controller.dart:458 importAudioFile` → `:440 addAudioLayer` | — |
| **ASSETS — Fonts** | ESCONDIDO | `FontService.instance.loadAll()` roda no arranque (`main.dart:56`) e carrega o acervo; nenhum seletor de fonte na UI nova — grep `FontService` em `presentation/` = 0, e a categoria de texto e so `_Texto` (`controles_da_camada.dart:85`) | Na categoria `'texto'` do `PainelDaCamada` (`painel_da_camada.dart:124-129` → `controles_da_camada.dart:85`), como uma lista horizontal de familias. O motor ja aceita: `editor_controller.dart:5608 editTextLayer`. |
| **ASSETS — 3D Models (importar)** | ESCONDIDO | `editor_controller.dart:1714 addModel3D` e `:1675 addGlbNode` tem **0 chamadores**; `ModelImportService` so e usado por fabricas de projeto (`projects/application/modelos_empacotados.dart:6`, `campo_assets.dart:8`), nunca pela UI. A familia "3D" do painel so oferece cena, nulo, 17 primitivas e particulas (`adicionar_conteudo.dart:120-145`) | Mais um `ItemDeAdicao` na categoria `'3d'` (`adicionar_conteudo.dart:120`), como o de imagem/video: `criar:` chamando o seletor de arquivo e depois `addModel3D`. |
| **ASSETS — Search** | ESCONDIDO | `editor_controller.dart:627 searchLayers` (+ `:636 _matchesSearch`, completo) tem **0 chamadores**. Nao ha busca de asset nem de camada em lugar nenhum | `VisaoGeralDasCamadas` (`visao_geral_das_camadas.dart:109`), a pilha, e onde uma busca de camada serve. Busca de ASSET so depois de existir biblioteca. |
| **ASSETS — Favorites** | ESCONDIDO / morto | `application/ui/effect_favorites.dart:9 EffectFavorites` (chave de prefs, conjunto de ids) tem **zero** uso no app | O catalogo de efeitos ja desenhado em `controles_da_camada.dart:930-985` — um chip "Favoritos" antes da lista, como o proprio comentario do arquivo previa (`effect_favorites.dart:7`). |
| **ASSETS — Recent** | FALTA (para midia) / N/A conceitual | existe "Recentes" para PROJETOS (`projects/presentation/projects_tab.dart:504`), nao para assets; nao ha historico de midia importada no motor | So faz sentido junto com a biblioteca (linha "Media"); mesma casa: `PainelCentralDeAdicao`. |
| **ASSETS — Import (imagem/video/audio)** | PASS | `adicionar_conteudo.dart:218,231,244,250` → `media/application/media_import_service.dart:24-34` (`pickVideoFromGallery`/`pickImageFromGallery`) com `persist()` (`:39`) copiando para fora do cache do seletor | — |
| **ASSETS — Import (SVG e icone)** | ESCONDIDO | `editor_controller.dart:316 addSvgLayers` e `:354 addIconLayer` tem **0 chamadores**; o `IconifyService` inteiro (`application/iconify_service.dart`) nao e referenciado por ninguem | Dois `ItemDeAdicao` novos: SVG na familia "Formas" e icone numa familia propria, ambos em `adicionar_conteudo.dart:160-208`. |
| **ASSETS — Replace** | FALTA | nao existe comando de trocar a fonte de uma camada: nao ha `setSourcePath`/`replaceMedia`/equivalente em `editor_controller.dart` (o `sourcePath` so entra na construcao, `layer.dart:595-600,722`). A categoria "Informacoes da midia" e SO LEITURA: `controles_da_camada.dart:991-1015` (`_Midia` monta tres `_Linha` de texto) | `_Midia` (`controles_da_camada.dart:991`) e o lugar obvio — a ficha ja mostra o arquivo; falta o botao ao lado. Precisa de um comando novo no motor. |
| **ASSETS — asset faltando** | **QUEBRADO** | imagem sumida vira triangulo de aviso (`palco_de_previa.dart:4121` → `:4265 _brokenMedia`) e video sem tocador vira icone de filme (`:4144-4155`) — e NENHUM dos dois pergunta se esta exportando, ao contrario do gizmo do nulo, que ganhou essa guarda (`:4171 NullLayer _ when exporting`). O aviso e ASSADO no arquivo entregue. Nao ha nenhum aviso fora da previa, nem relink | Guarda `when exporting` nos dois ramos (`palco_de_previa.dart:4116` e `:4144`), e um aviso na pilha de camadas (`visao_geral_das_camadas.dart:330`) — a pilula ja carrega identidade da camada. |
| **UNDO — Undo / Redo (global)** | PASS | motor `editor_controller.dart:210 undo()` / `:217 redo()`; UI `linha_do_tempo.dart:343-347` e `:349-353` (alvos 1 e 2 do transporte, com `canUndo`/`canRedo` em `editor_controller.dart:83-84`) | — |
| **UNDO — cobertura por dominio** | PASS por construcao (com ressalva dura) | funil unico: `editor_controller.dart:90 _mutate`; as UNICAS atribuicoes diretas de `state` sao `:125` (dentro do `_mutate`), `:213`/`:220` (undo/redo), `:232` (`openProject`), `:539`/`:556` (entrar/sair de grupo, que salvam e restauram as pilhas de proposito, `:520-566`). Ressalva: so vale onde HA porta — cena 3D, particulas, elemento 3D, mascara e animador de texto desfazem perfeitamente e nao tem UI nenhuma (`updateScene3D`, `addSceneNode`, `updateParticles`, `updateElement3D`: 0 chamadores em `presentation/`) | — |
| **UNDO — um gesto, um passo** | PASS | motor `editor_controller.dart:183 beginGesture` / `:192 endGesture` (+ auto-fecho de gesto orfao, `:92-97`); UI `controles_da_camada.dart:292-294, 389-391, 438-440, 478-480, 695-698` | — |
| **UNDO — cancelar gesto (desistir do arrasto)** | ESCONDIDO | `editor_controller.dart:202 cancelGesture` — escrito, correto (devolve o estado sem consumir passo) e com **0 chamadores** | `almofada_de_arrasto.dart` e `fita_de_ajuste.dart`, no `onPanCancel`/soltar fora da area: e exatamente o caso que o comentario do metodo descreve. |
| **UNDO — o botao sabe se pode desfazer** | funciona por ACIDENTE | `_Transporte` faz `ref.watch(editorControllerProvider.notifier)` (`linha_do_tempo.dart:339`), e observar o `.notifier` NAO notifica em mudanca de estado; o botao so acerta porque o pai `LinhaDoTempo` observa o PROJETO (`:123`) e reconstroi o filho a cada build. Alem disso `beginGesture` empilha sem mudar estado (`editor_controller.dart:183-190`), entao entre o inicio do gesto e a primeira mutacao o "Desfazer" fica apagado ja havendo passo | Trocar por um provider derivado (`canUndo`/`canRedo` observaveis) ou por `ref.watch(editorControllerProvider)` no proprio `_Transporte` (`linha_do_tempo.dart:337`). |
| **COPY/PASTE — texto, forma, video, audio, efeito, animacao, keyframes, objeto 3D, material** | FALTA (todos) | **nao existe area de transferencia de edicao em lugar nenhum do app.** Grep por `clipboard\|colar\|paste` em `lib/src/features/` so devolve copia de TEXTO: caminho do arquivo (`export/presentation/export_video_screen.dart:726`), relatorios (`settings/presentation/travadas_screen.dart:50`, `estresse3d_screen.dart:639`) e comunidade. Zero comandos de copiar/colar em `editor_controller.dart` | Comandos novos no motor (um buffer de `Layer`/`EffectInstance`/`List<Keyframe>` fora do `VideoProject`, para nao entrar no undo como estado). Na UI: `_AcoesDaCamada` (`controles_da_camada.dart:1055-1095`) ja e a gaveta de "o que se faz COM a camada" — Copiar/Colar entram ali, ao lado de Duplicar. |
| **DUPLICATE — camada (texto, forma, video, audio, objeto 3D, cena 3D)** | PASS | motor `editor_controller.dart:4269 duplicarCamada` (leva a ficha junto, `:4278`); UI em DOIS lugares: transporte `linha_do_tempo.dart:380-390` e `controles_da_camada.dart:1087` | — |
| **DUPLICATE — efeito** | ESCONDIDO | `editor_controller.dart:4555 duplicateEffect` — **0 chamadores** | A ficha do efeito na categoria `'efeitos'` (`controles_da_camada.dart:89` → `_Efeitos`), ao lado de remover/reordenar. |
| **DUPLICATE — keyframes** | FALTA | nao existe comando: o que ha e mover (`editor_controller.dart:4296 moverKeyframeDeTransformacao`) e apagar (`:4333 apagarKeyframeDeTransformacao`); nenhum copiar/duplicar/espelhar | `_FaixaState` (`linha_do_tempo.dart:774`), que ja pega o losango sob o dedo (`:800-825`) — cabe como toque longo sobre a marca. |
| **DUPLICATE — objeto 3D / camera de cena** | ESCONDIDO | `editor_controller.dart:2014 duplicateSceneNode` e `:2049 duplicateSceneCamera` — **0 chamadores** cada (e nao ha UI de cena 3D nenhuma na presentation nova) | Depende de a UI de cena 3D voltar; nao ha widget concreto para recebe-los hoje. |
| **DUPLICATE — material** | FALTA | nao ha comando de material separado do no (o material vive dentro de `node.material`, `palco_de_previa.dart:411`); nenhum duplicar/copiar de material em `editor_controller.dart` | idem: sem UI de cena 3D nao ha onde por. |
| **DUPLICATE — animacao / preset** | ESCONDIDO | `editor_controller.dart:700 saveEffectPresetFrom` e `:725 applyPreset` — **0 chamadores**; `application/effect_preset_store.dart` sem nenhum uso no app | O catalogo de efeitos (`controles_da_camada.dart:930`) — "salvar como predefinicao" e "aplicar predefinicao" sao dois chips. |
| **codigo morto correlato — `duplicateLayer`** | ESCONDIDO (morto) | `editor_controller.dart:3717 duplicateLayer` e um SEGUNDO comando de duplicar, com 0 chamadores, e que NAO copia a ficha (meta) — exatamente o defeito que o `duplicarCamada` documenta ter corrigido (`:4265-4268`) | Apagar. Manter dois duplicares e garantir que um dia o errado sera chamado. |

### Os tres achados mais graves

**1. A exportacao nao tem ajustes — nao "estao escondidos", eles nunca sao passados.** `ExportSettings` esta pronto, testado e completo (resolucao, fps, formato, codec, taxa de bits, sequencia PNG com alfa), e o unico lugar que abre a tela faz `const ExportVideoScreen()` sem argumento nenhum (`editor_screen.dart:182`). Todo mundo exporta 1080p-ou-o-que-o-projeto-for, H.264, "media", MP4, para uma pasta fixa — e nao existe caminho para nada mais. Pior: o fundo transparente esta **quebrado por baixo** mesmo se a porta existisse, porque a captura e sempre embrulhada em `ColoredBox(project.backgroundColor)` (`export_video_screen.dart:573`) com um fundo que nasce preto opaco e nao tem como ser mudado (`setBackgroundColor`: 0 chamadores). E o erro nao tem "tentar de novo" e o cancelar e um `x` de fechar janela.

**2. O relogio do editor nao e o relogio do projeto.** `PlaybackController.compositionFps` esta declarado como "a taxa da COMPOSICAO" e nunca recebe valor: fica em 30 para sempre (`playback_controller.dart:41`). O `seek` e o tick quantizam nessa grade (`:132`) enquanto a capsula CONTA os quadros pelo fps real do projeto (`linha_do_tempo.dart:671`). Num projeto a 60 fps o cabecote so pousa em quadro par e metade dos quadros e inalcancavel; a 24 fps o cabecote cai fora da grade do arquivo que sera exportado — que e exatamente o desastre que o comentario de `_naGrade` diz ter sido consertado. Todo o transporte, o scrubbing e a leitura de tempo herdam esse erro.

**3. Nao existe copiar e colar, e o que existe de mais so nao tem porta.** Copiar/colar de camada, efeito, keyframe, animacao ou material simplesmente nao esta no motor — nem um buffer, nem um comando (o unico `Clipboard` do app copia caminhos de arquivo). Ao lado disso, `ensureProxy` (o proxy que existe para o scrub ficar continuo em video pesado) nunca e chamado por ninguem, entao o sistema inteiro de proxy esta dormente e `setClipReverse` recusa em silencio todo clipe acima de 10 s (`editor_controller.dart:3077-3092`); `GalleryService`, `EffectFavorites`, `EffectPresetStore`, `IconifyService` e `EditorSession` inteiro sao codigo vivo sem um unico consumidor; e importar um video deixa a previa no icone de filme ate alguem arrastar o cabecote, porque `_syncVideos` so escuta o relogio e nao o projeto (`editor_screen.dart:47-48`).